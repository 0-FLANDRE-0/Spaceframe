# CHANGELOG

이 파일은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/) 형식을 따른다.
정식 버전이 나올 때마다 `Unreleased`의 내용을 새 버전 절로 옮기고, 같은 형식을
유지하며 갱신한다.

기준 파일은 저장소 최상위의 `Spaceframe_v<버전>.html` 하나다(현재
`Spaceframe_v1.31.38.html`). 버전명은 파일 내 `<title>`의 프로토타입
번호를 따른다.

---

## [Unreleased]

아직 없음.

---

## [1.31.38] — 2026-08-17

기준: v1.31.37 · tag `v1.31.38` · 실행 파일 `Spaceframe_v1.31.38.html`

1.31.37에서 첫 함재 미사일과 MFC, RAM/주파수별 RCS, 공통 관통 모델을 세운 뒤,
이번 버전에서는 그 기반을 실제 **유도탄 방어전·전자전·다중 레이더·중어뢰/핵탄두**까지
확장했다. ADS 근접방어용 에너지 개틀링과 전용 CIWS FCS, 패시브 IR 미사일, 복합 RF/IR
기만체와 광대역 노이즈 ECM, 외피 매립형 C-band AESA가 추가되며, 미사일·어뢰·기만체는
함선과 분리된 generic ordnance sensor track으로 관측된다. 1.8t 장거리 중어뢰와 0.25kt
핵탄두형도 같은 rigid-body/시커 계층에 들어왔고, 마지막 WIP에서는 핵폭발 시각효과만
실제 yield에 따라 스케일하도록 정리했다.

### 추가

- **ADS EG-2-591A 4연장 에너지 개틀링.** S2 패키지 하나에 4개의 순차 펄스 방출기를
  넣은 근접방어용 에너지 개틀링을 추가했다. 패키지 기준 최고 1,600RPM, 발당 48MJ
  캐패시터 요구, 30MJ급 포구 펄스를 사용하며 4개 방출기는 독립 열 풀을 가진다.
  과열된 방출기만 건너뛰어 연사율이 단계적으로 떨어지고 전체 패키지가 즉시 정지하지 않는다.
- **ADS CFC-1-590C CIWS 사격통제 노드.** S1급 전용 근접방어 FCS를 추가했다.
  자체 센서는 없으며 함선 공용 ordnance track을 받아 인접 CIWS 포탑 1기를 1:1로 통제한다.
  위협이 사거리로 들어오기 전에 pre-aim/pre-spin을 수행하고 실제 사격은 위험도·사거리·
  사격해 문턱을 모두 만족해야 허용된다.
- **S3 TUR-558D와 S2/S1 다연장 마운트.** `TUR-558D` 지원 규격을 S3/S4/S5로 확장했다.
  S3는 2×2m, 14t, 96°/s 프로필이며 S2 쌍열·3연장·4연장과 S1 4~8연장 마운트를 받는다.
  S2 4연장 마운트는 단순 4열 배치가 아니라 두 쌍의 겹친 hardpoint lane을 사용한다.
- **패시브 IR 미사일.** `ir-60k-passive-body`를 추가했다. 기존 ARH형의 물리 몸체·탄두·
  추진계를 공유하지만 능동 RF 시커를 제거하고 MWIR 비영상 스캔 시커를 사용한다.
  표적과 RF/IR 복합 기만체의 실제 열신호를 reticle error로 비교하며 RF 방출은 없다.
- **CMD-1 복합 대응책 사출기.** 1.25×1m S1 모듈에 RF/IR 복합 기만체 6기를 탑재한다.
  기만체는 32kg, 35m/s로 사출되고 6.5초 동안 존재하며 X-band에서 약 285m²의 유효 RCS와
  시간에 따라 감쇠하는 IR 신호를 제공한다. AUTO 외피 법선 또는 15° 수동 사출방향을 지원한다.
- **ECM-2-584A 광대역 노이즈 재머.** 2.4~12.4GHz 방향성 재머를 추가했다.
  고정 거리 디버프가 아니라 실제 수신기 J/N, `S/(N+J)`와 burn-through 관계로 레이더/ARH
  탐지를 방해하며 강한 재밍은 상대 레이더 전술 화면에 bearing strobe/noise wall로 나타난다.
- **RA-3-601A C-band 외피 매립형 AESA.** MGI S3 2×1m 고정식 단면 배열을 추가했다.
  5~7GHz, 기준 6GHz, 패널당 RF peak 14MW이며 설치면 기준 ±60° FoR 안에서
  SEARCH/TWS/ORDNANCE/FCS/STT 작업을 전자주사한다. 외피 고정형이라 실제 함체·격벽 차폐를 받는다.
- **다중 레이더 런타임.** 기존 2면 회전식 S-band PESA와 신규 C-band AESA를 한 함선에
  동시에 운용할 수 있다. 레이더 프로필별 런타임·채널·beam history를 독립 유지하고,
  한 장비가 STT를 맡아도 나머지 레이더는 가능한 탐색/추적 작업을 계속한다.
- **Generic ordnance sensor fusion.** 미사일·어뢰·기만체 같은 유도탄/ordnance를 위한
  센서 대상 어댑터와 공용 트랙 저장소를 추가했다. RADAR + EO/LRF + IR + ESM을 같은 트랙에
  융합하며, EO/LRF만으로도 조건을 만족하면 ranged fix와 운동 이력을 만들 수 있다.
  반대로 ESM/IR 같은 수동 방위만 있을 때는 임의의 거리를 만들어내지 않는다.
- **1.8t 장거리 중어뢰.** `heavy-torpedo-1800-arh`를 추가했다. 1,800kg 발사질량,
  850kg 추진제, 60kN 정격 재점화식 액체 주추진계와 최대 180kN의 짧은 기동 surge,
  자세 RCS/DACS, ARH 종말시커를 사용한다. 장거리에서는 DL/INS를 받고 종말에서는
  목표 속도를 한 번 더 더하지 않는 constant-velocity collision-course lead를 사용한다.
- **0.25kt 핵탄두 중어뢰.** 동일한 1.8t 비행체/추진/시커를 쓰고 탄두/신관만 바꾼
  `heavy-torpedo-1800-arh-nuclear-025kt`를 추가했다. 80m 표면 기준 근접신관과 충격신관,
  0.25kt(1,046,000MJ) yield를 가지며 감마선·열복사/ablation·근접 구조파쇄를 별도 피해
  채널로 처리한다. 장갑/격벽은 prompt radiation 차폐에 참여하고 RAM/라이너도 근접 열·구조
  피해를 받을 수 있다.
- **TARGET XRAY 디버그.** AI 디버그에서 락온 표적 인디케이터에 실제 내부 구조,
  모듈/장갑/격벽 손상, 주 관통선과 파편 흔적을 겹쳐 볼 수 있는 개발용 표시를 추가했다.
  정상 센서/BDA 정보와 분리되어 있으며 일반 전장 화면에 적 truth를 추가로 노출하지 않는다.

### 변경

- **미사일/어뢰 시커 기억과 재획득.** ARH·IR 시커가 TRACK을 잃으면 마지막 onboard
  센서 추정의 위치·속도·가속도를 제한 시간 동안 전파해 `MEMORY_TURN → REACQUIRE`를 시도한다.
  기억 중 가속도 신뢰도와 위치 정확도는 시간에 따라 떨어지며 live target truth를 읽지 않는다.
  중어뢰는 큰 방향전환이 필요한 memory turn에서 짧은 고추력 surge를 사용할 수 있다.
- **미사일 유도 권한 확장.** APN + 단계식 sustain throttle, 남은 추진제/Isp에서 계산한
  launch ΔV, pre-boost 정렬, boost vector capture를 하나의 유도 흐름으로 정리했다.
  sustain은 15/35/65/85/100%의 이산 출력 단계를 사용하고 센서 추정 가속도가 오래되거나
  불확실하면 APN 기여를 낮춘다.
- **CIWS 위협 스케줄러.** 여러 CIWS가 여러 ordnance track을 분배받고 commitment hysteresis를
  사용하도록 했다. 중어뢰처럼 큰 위협에는 복수 방어기를 집중할 수 있으며, 위협 점수 계산은
  적 유도탄의 warhead/profile truth를 직접 읽지 않는다.
- **CIWS 회전체/열/전력 계약.** 회전체 RPM과 실제 사용할 수 있는 방출기 수에 따라 순간
  발사율과 캐패시터 처리 요구량이 같이 변한다. 전 방출기 과열 상태에서는 스케줄러 슬롯을
  소비하지 않고, 냉각 후 과거의 비정상적으로 긴 cooldown sentinel이 남지 않도록 정리했다.
- **레이더 전술 표시.** 고정식 AESA는 가짜 360° 원이 아니라 실제 설치 boresight와 ±60° FoR를
  그린다. PESA/AESA의 beam history와 RF 상태는 장비별로 유지하고 전술 표시는 복합 상태를 합성한다.
- **관통/파편 예산.** 주 관통체가 내부 격벽·모듈과 만나는 접촉점을 별도로 기록한다.
  피격 한 번 전체에 하나의 파편 상한을 두는 대신 접촉별 soft/hard/emergency 예산을 적용해
  깊은 관통에서도 각 구조물의 후방파편 생성이 서로 독립적으로 제한된다.
- **관통 시각화.** 내부 격벽/모듈 관통점과 주 관통 경로를 락온/디버그 도해에 표시하며,
  표식 반경과 선 두께를 줄여 여러 접촉이 겹칠 때의 시각적 혼잡을 완화했다.
- **폭발 장갑 footprint.** 근접 폭발이 장갑 한 셀에만 피해를 몰아주지 않고 실제 폭발점 주변의
  여러 armor cell에 거리·노출·레이어 전달률을 적용해 피해를 분배한다.
- **핵폭발 yield 기반 시각 스케일.** 0.25kt를 기준으로 flash/ring 반경은 yield의 세제곱근으로
  확대·축소하고, 지속시간·선 두께·광도는 더 완만한 지수와 상·하한을 사용한다.
  **이 변경은 핵피해 계산을 건드리지 않고 시각효과에만 적용된다.** 기준 0.25kt에서는 기존
  260m flash, 2,600m ring, 2.8초 지속시간을 그대로 보존한다.

### 수정

- **중어뢰 종말 lead 이중 선행 문제.** 목표의 횡속도를 미래 aim point와 상대 희망속도 양쪽에
  중복 적용하지 않도록 정리해, 종말 유도가 실제 collision-course 상대속도를 만들게 했다.
- **고속 선회/재획득 안정성.** 큰 오차에서 중어뢰가 정격 추력만으로 방향을 바꾸느라 지나치게
  둔해지는 경우를 기동 surge로 보완하면서, 진입/이탈 문턱·펄스·cooldown으로 상시 180kN 운용을 막았다.
- **빈 미사일 발사대 기본값.** 신규 발사대는 특정 탄종이 자동으로 들어간 상태가 아니라 빈 상태에서
  시작하며 사용자가 설계 단계에서 미사일/어뢰와 수량을 선택하도록 계약을 바꿨다.
- **정식 승격 하네스 구조.** 후반 WIP 하네스 일부가 메인 IIFE 밖의 별도 `<script>`에 놓여 내부
  함수에 접근하지 못하던 테스트 구조를 정리했다. 정식본은 다시 단일 `<script>`/단일 IIFE 안에서
  게임 코드와 자체 하네스가 같은 내부 계약을 검사한다.
- **후속 WIP와 모순된 낡은 단언 정리.** 초기 미사일/기만체/센서융합/레이더/CIWS 테스트 중
  과거 `profile.status`, 예전 수치, 리팩터링 전 함수명이나 source grep, 설계 원본 fixture를
  검사하던 항목을 최종 런타임 동작/프로필 계약 기준으로 교체했다. 게임 로직을 옛 테스트에 맞춰
  되돌린 변경은 없다.
- `decoyLauncher`가 실제 모듈 레지스트리에는 존재하지만 공통 설치 타입 목록에서 빠져 있던
  구조 계약을 맞췄다.

### 저장 호환

- `MODULE_STORAGE_SCHEMA_VERSION`은 **42를 그대로 유지한다.**
- 1.31.37에서 추가한 미사일 발사대/MFC/RAM 저장 계약은 변경하지 않는다.
- 신규 CIWS·ECM·대응책 사출기·고정식 AESA는 기존 모듈 프로필 저장 경로를 사용하고,
  미사일 발사대의 탄종 프로필 선택으로 신규 IR 미사일과 중어뢰/핵탄두형을 저장한다.
- 기존 1.31.37 설계는 신규 제품을 자동으로 삽입하지 않은 채 현재 구성을 그대로 불러온다.
- 반대로 1.31.38 전용 제품 ID나 신규 유도탄 프로필을 사용한 설계를 1.31.37에서 다시 여는 것은
  호환 대상으로 보장하지 않는다.

### 검증

- JavaScript syntax **PASS**.
- 정식 파일 표시를 `SPACEFRAME GRID PROTOTYPE 1.31.38` / `PROTO 1.31.38`로 확인했다.
- 정식 HTML은 `<script>` **1개**, 브라우저 노출 `window.__SPACEFRAME_*_TEST__` 하네스 **185개**다
  (v1.31.37의 118개에서 67개 증가).
- 신규 67개 하네스 **488/488 PASS**. 미사일/시커/대응책/EW, ordnance sensor fusion,
  ADS CIWS, 고정식 AESA/다중 레이더, 중어뢰·핵탄두, 관통/파편/폭발 footprint,
  seeker memory와 핵 yield 시각 스케일까지 포함한다.
- 추가 영향권 21개 하네스 **542/542 PASS**: Product Metadata 144/144, Registry 98/98,
  REG-F 60/60, REG-G 49/49, System Adapter 37/37, Sensor-FCS A1 12/12,
  Sensor-FCS B1 11/11, EO/BDA 9/9, Radar Physics 13/13, Radar-FCS 9/9,
  Power Foundation 7/7, Power Balance 10/10, Mass Balance 8/8, Price Integration 9/9,
  Ship Cost 4/4, Product Naming 33/33, Company DB 6/6, UI-I1 11/11,
  Catalog Readability 4/4, RCS Edge 5/5, Storage Codec 3/3.
- 정식 승격용 신규/영향권 검증 합계 **1,030/1,030 PASS** (88개 하네스 실행).
- 일반 브라우저 로드 스모크에서 page error **0**, console error **0**, 노출 하네스 **185개**를 확인했다.
- 전체 185개 하네스를 한 페이지에서 직렬 실행하면 성능/물리 하네스의 전역 상태와 실행시간이
  누적되므로 정식 판정 기준으로 사용하지 않았다. 신규·영향권 하네스는 격리 또는 소그룹 실행으로 검증했다.

### 알려진 제한

- 함재 유도탄 제품군은 현재 ARH/패시브 IR 미사일과 1.8t ARH 중어뢰 계열이 중심이며,
  SARH 실탄과 더 다양한 미사일/어뢰 체급은 아직 없다.
- 핵탄두는 현재 0.25kt 중어뢰형 한 종류다. 감마·열·구조파쇄를 게임 물리로 분리했지만
  실제 핵효과 전체를 정밀 복사수송/유체역학으로 푸는 모델은 아니다.
- 대응책은 현재 CMD-1 RF/IR 복합 기만체, ECM은 ECM-2 광대역 노이즈 재머 한 종류다.
  채프 구름, DRFM 거리/속도 기만, 복수 ECM 제품군은 아직 없다.
- 실제 레이더 제품은 기존 S-band 회전식 PESA와 신규 C-band 외피 고정 AESA 두 계열이다.
  다중 레이더 런타임은 확장 가능하지만 더 많은 배열/대역 제품은 이후 추가 대상이다.
- `TARGET XRAY`는 의도적으로 enemy truth를 읽는 개발용 디버그 기능이며 정상 센서/BDA 게임플레이
  정보로 취급하지 않는다.

---

## [1.31.37] — 2026-08-15

기준: v1.31.36 · tag `v1.31.37` · 실행 파일 `Spaceframe_v1.31.37.html`

1.31.36이 전투 프레임 저하와 테스트 기반을 정리한 성능 릴리스였다면, 이번 버전은
다시 **설계 작업 흐름·함재 미사일·RAM/주파수 RCS·관통/2차 피해 물리**를 한꺼번에
확장한다. 첫 4연장 함재 미사일 발사대와 전용 MFC가 실제 전투 루프에 연결되고,
장갑 바깥의 RAM 표면처리가 주파수별 RCS와 EO 대비에 들어간다. 동시에 운동탄·미사일
관통을 하나의 동적 모델로 통합하고 APHE와 초고속 충돌 후방효과까지 같은 피해 경로에
올렸다.

### 추가

- **설계 도구 3개 탭.** 하단 도구를 `기초 / 무장 / 고급`으로 분리했다.
  `기초`는 함체·장갑·격벽·반응로·엔진·RCS·캐패시터·보호막,
  `무장`은 함체 고정무장·포탑·미사일 발사대·캐패시터·탄약고·FCS,
  `고급`은 IR/EO·레이더·RAM·ESM·센서 통합·FCS·열관리 장비를 보여준다.
  캐패시터/FCS처럼 역할이 겹치는 장비는 여러 탭에 나타나지만, 무장·포탑·발사대·
  탄약고는 무장 탭에만 둔다.
- **4연장 발사대.** `missile-launcher-quad-arh40`을 추가했다. 5×1m, 1m 두께의
  고정식 2×2 캐니스터 4셀 구조이며 기본 40km급 ARH 미사일 4발을 통합 탑재한다.
  전면 도어는 발사축 기준 외피에서 최대 1.25m까지 후퇴 설치할 수 있다.
- **모바일 미사일 발사대 배치 계약.** 긴 발사대를 배치할 때 첫 탭은 ghost를
  주차하고, 빈 화면 드래그는 계속 카메라 이동으로 사용한다. ghost 자체를 드래그해
  재배치하고 나중에 ghost를 탭해야 실제 설치되므로 터치 환경에서 화면 이동과 설치가
  충돌하지 않는다.
- **미사일 탑재 편집기.** 설치된 발사대 정보창에서 호환 미사일 프로필과 실제 탑재
  수량을 선택할 수 있다. 선택한 `missileProfileId`, 잔탄, MFC 자동/수동 연결 상태가
  설계 저장에 포함되고 대칭 편집 시 반대편 발사대에도 같은 탑재안이 적용된다.
- **MFC-1-582A 미사일 사격통제 노드.** 탄도 FCS와 미사일 FCS의 역할을 제품 프로필
  수준에서 분리했다. MFC는 함선 공용 센서 트랙을 받아 ARH 발사해와 DL/INS 중간유도
  패킷을 만들며 최대 8개의 ARH DL 채널, 10Hz 갱신을 제공한다. 레이더 장착 여부와
  독립적으로 EO/LRF 등 유효한 정밀 트랙도 사용할 수 있다.
- **40km급 ARH 미사일 물리체.** 포탄 배열과 분리된 독립 rigid-body 비행체를 추가했다.
  280kg 발사질량, 4.2m × 0.28m 몸체에 95kN/2.2s 고체 부스터, 14kN급 4분할
  multi-pulse sustain, TVC·자세 RCS·DACS를 사용한다. 각 추진계는 실제 추진제 질량을
  소비하며 질량·관성도 비행 중 갱신된다.
- **미사일 유도/시커.** PN 기반 요격 유도, 함선 sensor track을 복사한 DL packet,
  packet 사이 INS coast, 링크 상실 coast, X-band 능동 레이더 시커의 SEARCH/TRACK/
  COAST/LOST와 datalink → ARH handoff를 구현했다. 미사일 guidance가 표적의 실제
  truth 좌표·속도를 직접 읽어 넣는 경로는 사용하지 않는다.
- **미사일 발사 모드.** 정밀 ranged track과 작동 중 MFC가 있으면 `DL`로 발사한다.
  MFC가 있으나 거리 없이 RADAR/EO/IR 방위만 있으면 발사관 이탈 후 해당 방위로 선회해
  ARH SEARCH를 시작하는 **bearing-cued MAD**를 사용한다. MFC가 없거나 파괴되면
  센서 cue가 차단되고 발사관 축 방향 **MAD DOG**만 허용된다. HUD 표기는 `DL / MAD`
  두 상태로 단순화했다.
- **전술맵 자함 미사일 텔레메트리.** 자함이 발사한 미사일만 전술 그림에 표시한다.
  적 미사일은 향후 센서 접촉 계층을 거쳐야 하며 enemy missile truth를 UI에 직접
  그리지 않는다.
- **미사일 관통 HE-FRAG 탄두.** 18kg `military-he-standard` 작약과 40kg 케이싱을
  사용하는 관통형 탄두를 추가했다. 안전분리 시간/거리는 폭발 신관만 억제하며,
  너무 가까이 충돌해도 미사일 몸체의 실제 운동 충돌은 그대로 발생한다.
- **범용 광대역 RAM 패널.** 장갑 `layers[]`와 분리된 surface treatment 레지스트리를
  추가했다. 현재 MGI 범용 RAM은 12mm 기준 25kg/m² · 45kCr/m²이며 5~20mm 두께를
  지원한다. X/Ku 부근에서 흡수가 가장 강하고 저주파·초고주파로 갈수록 약해진다.
- **RAM 설치/제거·손상.** 외장 장갑 최외곽에 구간 단위로 RAM을 설치/제거할 수 있다.
  RAM은 자체 질량·가격·미약한 물리 저항과 damage cells를 가지며, 국부 파손된 면은
  원래 장갑의 RCS/가시광 특성으로 돌아간다.
- **RCS 주파수 응답.** S/X/Ku/Ka/W 밴드 레지스트리와 실제 Hz 기반 연속 응답 모델을
  추가했다. 3.0GHz에서는 1.31.36 방향별 RCS를 정확히 보존하고, 다른 주파수에서는
  diffuse/specular/edge/module 성분을 각각 log-frequency 보간한다. 밴드 경계에
  RCS가 계단식으로 튀지 않는다.
- **RCS 주파수 분석 UI.** 설계 CS/RCS 오버레이에 0.1~300GHz 분석 슬라이더, 대역
  바로가기와 방향별 주파수 곡선을 추가했다. 300GHz는 엔진 하드 상한이 아니며
  검증 범위를 벗어난 요청은 끝점 응답을 유지해 외삽 폭주를 막는다.
- **150mm APHE.** 52kg 관통체·2,040m/s의 150mm 철갑고폭탄을 추가했다. 단일
  접촉 저항이 100mm RHAe 이상이면 신관이 무장되고 0.9ms 뒤 폭발한다. 7kg 작약과
  45kg 케이싱 파편은 탄체의 잔존 전진속도와 방사 속도를 벡터 합성한다.

### 변경

- **운동탄 관통을 authoritative 동적 모델로 통합.** Tungsten sabot, 60mm APFSDS,
  150mm APHE, VHI 132mm HVAP, HPS 220mm hypervelocity penetrator와 미사일 관통체가
  같은 `penetration family + material + shape + geometry + current impact state`
  경로를 사용한다. 고정 관통 mm를 런타임 판정값으로 저장하지 않으며 UI의 기준 RHAe도
  같은 모델을 표준 조건에서 계산한다.
- **관통 형상/재질 계약.** Long-rod, full-bore, composite-core, hypervelocity,
  penetrating-body 계열을 분리하고 `normalPenetrationFactor`, 경사 유지력, bite,
  ricochet resistance, normalization, tip erosion/hypervelocity retention 등을
  실제 관통부 형상에 둔다.
- **HVAP/APCR 코어·캐리어 분리.** 비행과 보호막 충돌에서는 완성탄 질량/외경을 쓰지만
  장갑 관통은 작은 고밀도 코어가 담당한다. 첫 관통 뒤 캐리어는 코어에 붙어 남는 질량,
  후방 debris, 전면에서 탈락한 질량으로 분리되며 다음 구조물 관통은 남은 상태에서
  다시 계산한다.
- **초고속 충돌 후방효과.** Hypervelocity 탄의 관통 깊이와 주변 피해를 분리했다.
  대표 파편 ray 수를 늘리지 않고 충돌 과정에서 침적된 운동에너지에 따라 각 ray가
  대표하는 debris cloud의 에너지와 피해 상한을 키운다.
- **진공 파편 운동 계약.** 대표 파편 ray를 무조건 14m에서 삭제하던 하드캡을 제거했다.
  진공 비행 자체로 에너지/속도를 잃지 않고, 격벽·모듈·라이너 등 실제 물체와 충돌할 때만
  감소한다. 함체를 빠져나가거나 유효 에너지/속도 이하가 되면 종료한다.
- **FCS 자동사격 신뢰도.** 전투가 2D 평면이고 투사체 분산도 하나의 횡방향 각도
  Gaussian이므로, 기존 2D 원형 radial CDF 대신 **1D lateral Gaussian가 표적 반폭
  안에 들어올 확률**을 사용한다.
- **탄종 변경 공통 계약.** 플레이어와 AI가 같은 `setWeaponGroupAmmo()` 경로를 사용하며
  탄종 변경 시 3.8초 그룹 재장전을 시작하고 기존 FCS 해를 즉시 폐기한다. AI는 관측된
  BDA를 바탕으로 150mm sabot/APHE를 시험·전환하고 비관통이 관측되면 다시 전환할 수 있다.
- **RAM 광학 저감과 geometric CS 분리.** 현재 RAM의 가시광 대비 저감은 두께와 무관한
  제품 특성으로 취급하고, geometric cross-section 자체는 바꾸지 않는다. RAM 손상이
  생긴 면적만 광학 저감 효과가 사라진다.
- 레이더 카탈로그와 전술 레이더 UI가 실제 운용 주파수, S-band 분류, 파장과 beam별
  현재 RF 정보를 더 직접적으로 표시한다.

### 수정

- **미사일 물리 WIP14/WIP15의 썩은 단계명 단언 2건.** 실제 TVC/RCS와 multi-pulse
  동작은 정상인데 테스트가 `profile.status`의 과거 개발단계 문자열을 하드코딩하고 있어
  후속 WIP21/25 상태명으로 바뀐 뒤 실패했다. 단계 이름을 검사하지 않고 실제 추진·자세제어
  프로필 계약과 동작을 보도록 교체했다.
- **미사일 발사대 모바일 배치 충돌.** 긴 launcher ghost가 존재할 때도 빈 화면 드래그는
  카메라 팬으로 유지하고, ghost 위에서만 재배치/확정 제스처를 받도록 분리했다.
- **탄속이 다른 탄종 전환 뒤 낡은 FCS 해가 남을 수 있던 경로.** 탄종 변경 진입점에서
  해당 무장 그룹의 사격해와 타이머를 폐기하도록 통일했다.

### 저장 호환

- 설계 저장 스키마를 **39 → 42**로 올렸다.
- 미사일 발사대에 `missileProfileId`, `missileRounds`, `missileFcsLinkMode`,
  `missileFcsModuleId`를 저장한다.
- FCS 모듈은 `fcsProfileId`로 탄도 FCS와 MFC 제품 역할을 구분한다. 해당 필드가 없는
  구형 FCS는 로드 시 기존 탄도 FCS로 복원된다.
- RAM은 장갑 wall의 `surfaceTreatment`에 제품 ID, 두께와 damage cells를 저장한다.
  장갑 본체의 `layers[]`와는 별도 계약이다.
- 기존 1.31.36/스키마 39 설계에는 위 신규 필드가 없으므로 기존 모듈과 장갑은 그대로
  복원되며, 새 미사일 발사대·MFC·RAM은 자동으로 추가하지 않는다.
- 반대로 1.31.37에서 새 프로필/표면처리를 사용한 설계를 1.31.36으로 다시 여는 것은
  호환 대상으로 보장하지 않는다.

### 검증

- JavaScript syntax **PASS**.
- 정식 파일 표시를 `SPACEFRAME GRID PROTOTYPE 1.31.37` / `PROTO 1.31.37`로 확인했다.
- 페이지에 `window.__SPACEFRAME_*_TEST__` 형식의 자체 하네스가 **118개** 노출된다
  (v1.31.36의 85개에서 33개 증가).
- 신규 33개 하네스 **424/424 PASS**. 설계 탭/미사일 적재, 미사일 발사대·MFC·
  rigid-body/추진/유도/시커/탄두, RAM/RCS 주파수, APHE, authoritative penetration,
  초고속 피해와 진공 파편 계약을 포함한다.
- 추가 영향권 회귀 **496/496 PASS**: Product Metadata 118/118, Registry 88/88,
  REG-F 54/54, REG-G 45/45, System Adapter 37/37, Sensor-FCS A1 12/12,
  Sensor-FCS B1 11/11, EO/BDA 9/9, Radar Physics 13/13, Radar-FCS 9/9,
  Power Foundation 7/7, Power Balance 10/10, Mass Balance 8/8,
  Price Integration 9/9, Ship Cost 4/4, Product Naming 33/33,
  Company DB 6/6, UI-I1 11/11, Catalog Readability 4/4, RCS Edge 5/5,
  Storage Codec 3/3.
- 정식 승격 대상 신규/영향권 검증 합계 **920/920 PASS**.
- 위 브라우저 검증에서 page error **0**, console error **0**.
- IndexedDB 전체 보관함 하네스는 이 검증 환경의 `set_content` 보안 컨텍스트에서
  IndexedDB 접근이 차단되어 합계에서 제외했다. 스키마/직렬화 계약과 Storage Codec은
  별도 회귀에서 통과했다.

### 알려진 제한

- 현재 함재 미사일 제품은 40km급 ARH 프로토타입 한 종류이며 발사대도 4연장 고정식
  제품 하나뿐이다. 전투 중 재장전과 외부 탄약고 급탄은 없다.
- MFC는 ARH DL 최대 8발과 SARH 예약 채널 계약을 갖지만 현재 실제 미사일은 ARH 하나다.
- bearing-cued MAD는 RADAR/EO/IR 방위만 초기 cue로 사용한다. ESM-only 접촉을
  미사일 수동 cue로 사용하는 계약은 아직 만들지 않았다.
- RAM 제품은 현재 MGI 범용 광대역 한 종류다. 0.1~300GHz는 현재 검증/시각화 범위이며
  그 밖의 주파수는 끝점 응답을 유지하는 근사다.
- RCS 주파수 모델은 full-wave EM 해석기가 아니라 기존 방향별 equivalent RCS를
  주파수별 산란 성분 응답으로 확장한 게임 수준 모델이다.

---

## [1.31.36] — 2026-08-13

기준: v1.31.35 · tag `v1.31.36` · 실행 파일 `Spaceframe_v1.31.36.html`

**게임 규칙과 화면은 바뀌지 않았다.** 이 버전은 두 갈래 작업을 담는다 — 1.31.35의 하네스를
돌려 나온 단언 실패 4건이 전부 게임 결함이 아니라 **테스트가 썩은 것**이었고 그것을 동작 기반
단언으로 바꾼 일, 그리고 전투 중 프레임 저하를 잡는 성능 작업이다. 성능 쪽은 세 라운드가 각각
다른 구간을 겨냥했고 모두 **동작 변화 0 / 픽셀 변화 0**을 검증한 뒤에만 남겼다. 효과가
측정되지 않은 시도는 되돌렸다.

---

### 1부 — 썩은 테스트 단언 4건


**게임 동작은 바뀌지 않았다.** 1.31.35 전체 하네스를 돌려 나온 단언 실패 4건이
모두 게임 결함이 아니라 **테스트가 썩은 것**이었고, 이번 버전은 그 4건을 앞으로
다시 썩지 않도록 동작 기반 단언으로 바꿨다. 실패 4건의 성격은 두 가지였다 —
소스 문자열을 grep 하거나 상수를 손으로 못 박아 정당한 변경에 걸려 넘어진 결정적
실패 3건, 그리고 난수 편향을 고정하지 않아 확률적으로 실패하던 1건이다.

### 수정

- **`preview-shows-breech-and-barrel` (WEAPON-PHYSICS-WIP6).** 그리기 함수의
  소스에서 `['barrel',g.barrel]` 리터럴을 찾고 있었는데, VHI HG-132/3 3포신을
  지원하려고 파트 목록을 다포신 대응으로 바꾸면서 그 문자열이 사라졌다. 파트
  목록을 순수 함수 `weaponPlacementPreviewParts(g)`로 분리하고, 단언은 계약
  자체(약실 1개 + 포신 N개, 각 사각형이 유효하고 포신끼리 위치가 겹치지 않을 것)를
  본다. 다포신 표본도 id를 못 박지 않고 제품 목록에서 찾는다.
- **`schema-contract` (REG-F).** `MODULE_STORAGE_SCHEMA_VERSION===37`로 못 박혀
  있었으나 커패시터·RCS 추가로 정당하게 두 번 올라 39가 된 상태였다. 번호 자체
  대신 **썩지 않는 계약**을 본다 — 정수·하한, 그리고 실제 저장 payload
  (`emptyDesign`/`compactDesign`/`cloneDesign`/`designPairPayload`)에 찍히는
  번호가 상수와 어긋나지 않을 것.
- **`eo-stage5-uses-bda-snapshots` (SENSOR-EOIR-E1).** 전장 화면
  (`drawObservedShipPartial`)이 BDA를 그리는지 grep 하고 있었는데, 1.31.35에서
  BDA를 **의도적으로** 전장 화면에서 빼고 락 지시기 도해에만 남겼다. 즉 이 단언은
  이미 제거된 동작을 요구하며 `TARGET_LOCK_WIP18`과 정면으로 모순됐다. 오프스크린
  캔버스에 5단계 도해를 실제로 그려 픽셀을 해싱하는 방식으로 바꿨다 — 트랙의 BDA
  스냅샷을 바꾸면 결과가 바뀌고, 같은 스냅샷이면 같으며, 스냅샷이 비면 아무것도
  그리지 않아야 한다.
- **`radar-track-projects-measurement-to-now` (RADAR-FCS-WIP6).** 허용오차
  15m를 손으로 적어 뒀지만 레이더 거리 측정 잡음은 트랙 고유 난수 편향
  (`_radarRangeBias`) × `sttRangeNoiseM:26`이라 최대 ±26m다. 같은 페이지에서 40회
  반복해 **5회(약 12%) 실패**를 실측했다. 테스트에서 편향을 고정값으로 지정하고,
  허용오차는 실제 잡음 상수에서 유도하도록 바꿨다.

### 내부 변경

- `weaponPlacementPreviewParts(g)` 신설. `drawWeaponPlacementIndicator`가 인라인으로
  갖고 있던 파트 목록 구성을 순수 함수로 분리했다. 그리기 동작은 동일하다 —
  고정무장 6종 전부에 대해 미리보기 렌더를 실측해 캡션과 픽셀 출력이 나오는 것을
  확인했다(3포신은 7,373px로 가장 넓다).

### 테스트·검증

- 하네스 81개 · **단언 1,372건 전부 통과 · 실패 0 · pageerror 0.** 총 단언 수는
  1.31.35와 동일하다(비감소 기준 충족).
- 하네스마다 페이지를 새로 여는 격리 실행으로 측정했다. 1.31.35에서 확인한 전역
  상태 누적(연속 실행 시 `PERF_H`가 20.6s → 70.9s로 3.4배) 때문에 순차 실행은
  판정 기준으로 쓰지 않는다.
- **변이 시험(mutation testing).** 새 단언 4건 각각에 대해 그것이 지키는 동작을
  사본에서 되돌려 봤다(파트 목록에서 다포신 제거 / `emptyDesign` 스키마 번호
  어긋나게 / 5단계 도해가 BDA 스냅샷을 소비하지 않게 / 레이더 fix를 200m
  밀어내기). 네 경우 모두 **의도한 단언 정확히 하나만** 실패했다.
- 레이더 하네스 40회 반복: **실패 0회**(이전 5/40).
- 페이지 로드 스모크: 캔버스 렌더 정상, pageerror 0.

### 알려진 문제

- 태그 push와 GitHub Release 생성은 이 세션 환경에서 불가하다(`CLAUDE.md`의
  "현재 환경에서 가능한 범위" 참조). 태그는 로컬에만 만들고, 대상 커밋 SHA를
  보고에 실어 사용자가 웹에서 만들도록 한다.

---

### 2부 — 전투 중 프레임 저하

**게임 규칙과 화면은 바뀌지 않았다.** 전투 중 프레임 저하를 잡는 성능 작업만
들어갔다. 세 라운드가 각각 다른 구간을 겨냥했고, 모두 **동작 변화 0 / 픽셀 변화 0**을
검증한 뒤에만 남겼다. 효과가 측정되지 않은 시도는 되돌렸다.

### 성능

- **AI 코어 타이머 위상 분산.** `updateUniversalAiCore`의 dynamic(10Hz)·world(10Hz)·
  planner(4Hz)가 항상 같은 프레임에 몰려 있었다. dynamic과 world는 주기가 같고 위상도
  같아 영원히 붙어 있었고, `startBattle`이 각 함선 코어를 `dt=1`로 프라이밍하면서
  누산기가 전부 정확히 0이 되어 **함선끼리도 겹쳤다.** 실측상 AI가 도는 프레임의 83%가
  4호출 이상 동시였다. 코어 생성 시 황금비 저불일치 수열로 모델별·함선별 시작 위상을
  배분했다. **갱신 주기는 그대로다** — 한 프레임에 몰리는 양만 줄였다.
  동시발화 최대 6 → 3(실전) / 6 → 2(합성), update p90 -5.5% · p95 -6.2%.
- **피격 2차 피해(파편) 큐.** 피격 1건이 최대 84개의 파편을 같은 프레임에 추적했다.
  `spawnSpallFragments`를 시작/1발 전진/완주로 쪼개고, 전투 중에는 큐에 쌓아 매 프레임
  **0.8ms 예산**만큼 소진한다. 표적이 파괴돼도 남은 파편은 계속 처리한다.
  `>12ms` 프레임 -43% · `>14ms` -67%(약 10,400프레임 합산 기준).
- **적 관측 함체 렌더 캐시.** `drawObservedShipPartial`이 매 프레임 `hullTiles`로 경로를
  다시 만들고 경계선마다 따로 stroke 했다(프레임당 lineTo 868 · moveTo 476 ·
  beginPath 285 · stroke 284 — 렌더러에서 가장 무거운 드로우콜). 함체 기하는 전투 중
  바뀌지 않으므로 `Path2D`와 좌표 배열로 캐시했다. 568 → 400 µs/frame(-31%).
- **`positionTargetAid` 강제 레이아웃 제거.** `style.top`/`height`를 쓴 직후
  `getBoundingClientRect()`를 불러 매 프레임 동기 레이아웃을 강제했다(1회 41.7ms 실측).
  결과가 `innerHeight`와 두 패널 class에만 의존하므로 그게 그대로면 건너뛴다.
  140 → 59 µs/frame(-58%).
- **함선 인디케이터 기하 캐시.** 8Hz로 다시 그려지는데 함체 타일·경계·모듈의 좌표는
  전투 중 바뀌지 않는다. 경로만 `Path2D`로 캐시하고 색은 매 호출 계산한다.
  1,854 → 1,411 µs/call(-24%). 프레임 단위 효과는 잡음 수준이라 주장하지 않는다.

종합해 `drawBattle`이 2,229 → 1,946 µs/frame(**-12.7%**)이 됐다.

### 수정

- **`position-is-reconstructible-from-published-measurements` 확률적 실패.**
  약 25% 확률로 깨지던 선재 결함이다(릴리스 커밋에서도 2/10 실패). `estimatedX/Y`는
  측정값을 현재 시각으로 투영한 값인데 단언은 투영하지 않은 원측정과 비교했다. 차이가
  정확히 `speed × age`였고, 루프가 레이더 재방문 주기의 어디서 끝나느냐에 따라 0~1초로
  흔들렸다. 발표된 속도까지 써서 투영을 재구성하도록 고쳤고 오차가 5m → 1m 미만으로
  **오히려 엄격해졌다.** 실패 시 원인을 알 수 있도록 detail도 채웠다.

### 내부 변경

- **피격 이벤트별 시드 RNG.** 2차 피해를 미루기 전에 결정론을 먼저 확보했다.
  `spawnSpallFragments`가 파편마다 전역 `Math.random()`을 3번, `maybeSpawnBackfaceSpall`이
  후면박리 발생 여부 자체를 전역 난수로 정하고 있었다. mulberry32 스트림을 피격 발생
  시점(계산 시점이 아니라)에 시드해 붙였고, 파편이 유발하는 연소 판정까지 `damageModule`에
  선택적 rng를 통과시켰다. 스트림이 없는 `trace`에는 그 자리에서 만들어 붙인다 —
  전역 난수 폴백으로 두면 rng를 빠뜨린 호출부가 조용히 비결정적으로 돌아간다.
- `weaponPlacementPreviewParts` · `observedHullPaths` · `indicatorGeometryCache` 신설.
- 인디케이터 기하 캐시의 키는 `map` 변환의 파라미터를 열거하지 않고 **변환 자체를
  세 점으로 탐침한다**(`map(0,0)`·`map(1,0)`·`map(0,1)`). 아핀 변환은 이 셋으로 결정되므로
  파라미터를 빠뜨려 캐시가 썩는 경로가 없다.

### 되돌린 것

- **상태 패널 위상 분산.** 근거로 삼은 `STATUS_PANEL_INTERVAL_MS`를 1Hz로 오독했으나
  실제로는 `1000/8`(8Hz)이었다. 넣어보니 143번 중 126번이 여전히 같이 터졌고
  `combatAid` p95도 안 내려갔다 — 스파이크의 정체는 두 패널의 겹침이 아니라 아군
  인디케이터 하나였다. 되돌렸다.
- **주 관통 경로 분할.** 최악 피격 11.1ms 중 46%가 주 관통 경로지만, 4프레임에 나누면
  관통 판정이 최대 4프레임 늦어지고(느릴 때 가장 심하게), 1,850RPM 개틀링에서 큐가
  발산하며, 진행 중 피격끼리의 읽기-쓰기 충돌을 막으려면 물리 함수 위에 트랜잭션
  계층이 필요하다. 2차 피해 큐 이후 남은 `>12ms` 프레임이 2,600프레임에 1번이라
  수지가 맞지 않아 착수하지 않았다.

### 테스트·검증

- 하네스 85개 · **단언 1,407건 전부 통과 · 실패 0 · pageerror 0.**
  1.31.36 기준선 1,372건에서 35건 증가(비감소 기준 충족).
- 신규 하네스 4종: `AI_PHASE_PERF`(7) · `IMPACT_DETERMINISM`(10) ·
  `SECONDARY_QUEUE`(10) · `INDICATOR_CACHE`(8).
- **변이 시험.** 새 단언마다 그것이 지키는 동작을 사본에서 되돌려 확인했다 —
  위상을 0으로 되돌리기, FIFO를 라운드로빈으로 바꾸기, 캐시 키에서 변환 제거,
  타일·모듈 손상 색 무시, 융합 추정을 적 실제 좌표로 덮어쓰기. 각각 의도한 단언
  하나만 실패했다.
- **픽셀 동일성.** 렌더를 건드린 두 변경은 오프스크린 캔버스로 그려 픽셀을 직접
  비교했다. 적 함체는 줌 3종 × 단계 3종, 인디케이터는 손상 4종 × 패널 크기 3종에서
  **전부 차이 0**이다.
- 짝지은 A/B를 반복 실행해 측정했다. 효과가 잡음에 묻힌 항목은 그렇게 보고했고,
  효과가 없던 시도는 되돌렸다.

### 알려진 문제

- 인디케이터 정적 층을 오프스크린으로 캐시하면 이론상 프레임당 약 300µs를 더 줄일 수
  있으나, 그때부터 손상 상태가 캐시 키에 들어가 **패널이 조용히 굳는** 실패 모드가
  생긴다. 나중에 착수할 때를 대비해 `INDICATOR_CACHE` 하네스가 모듈·프레임 타일·격벽 셀·
  장갑 셀 네 갈래를 각각 손상시켜 도해 픽셀이 바뀌는지 검사한다.
- 프레임 시간 측정은 헤드리스 소프트웨어 렌더 기준이라 절대값을 실기기에 그대로
  옮길 수 없다. 래스터 비용은 과장되고 JS·DOM 비용은 비교적 정확하다.
- 태그 push와 GitHub Release 생성은 이 세션 환경에서 불가하다. 태그는 로컬에만 만들고
  대상 커밋 SHA를 보고에 싣는다.

---

## [1.31.35] — 2026-08-11

기준: v1.31.34 · tag `v1.31.35` · 실행 파일 `Spaceframe_v1.31.35.html`

1.31.34에서 기업 DB와 제조사 메타데이터를 연결한 뒤, 이번 버전에서는 **기업별 실제
제품군을 처음 추가하고 포탑 규격 체계를 확장했으며, 센서 트랙만을 입력으로 사용하는
범용 전투 AI 코어를 실제 적함 비행제어에 연결했다.** HPS와 VHI의 대형 무장이 첫 고유
제품으로 들어오고, EO/LRF의 Stage 5 BDA와 복원된 락온 표적 전술도가 관측 가능한 피해
정보를 전투 UI에 제한적으로 보여준다.

### 추가

- **Universal Combat AI Core.** 함선 설계에서 무장 구성, 방향별 화력, 유효 사거리,
  탄약·열·전력 지속력과 전진/후진/횡가속·회전 권한을 읽어 Static/Dynamic Model을
  구성한다. 적 정보는 `sensorTracks`만 소비하는 World Model을 거치며 실제 적함의
  truth 상태를 직접 읽지 않는다.
- **AI Tactical Planner / Flight Intent.** 거리와 상대 방위 후보를 공격·생존·통제·지속력·
  정보·위험 항목으로 평가해 희망 거리, 희망 상대 방위와 접근/이탈 속도를 선택한다.
  충분한 TRACK에서는 실제 적함 비행제어를 이 Intent가 담당하고, 유효한 명령을 만들 수
  없으면 기존 `aiStableCombatCommand`가 fallback으로 남는다.
- **AI 디버그 패널.** 전투 화면에 `AI` 디버그 토글을 추가했다. SELF / WORLD / FIRE /
  PLAN / INTENT / CTRL 6개 보기를 통해 함선별 능력 모델, 센서 기반 접촉, 후보 점수,
  선택 Intent와 실제 자세/병진 제어를 확인할 수 있다.
- **포탑 S규격 체계.** S2~S10 설치 크기 레지스트리를 추가하고 포탑의 Series/Model과
  실제 설치 S규격을 분리했다. 현재 `TUR-558D`는 S4(3×3m)와 S5(4.25×4.25m)를 지원한다.
- **포탑 배치 전 크기 선택.** 포탑 카탈로그에서는 모델을 선택하고, 설계 하단의 작은
  배치 탭에서 S4/S5 규격을 `− / ＋`로 고른 뒤 배치하도록 변경했다.
- **확장 마운트.** 기존 S3 단장/쌍열에 S3 3연장, S4 단장, S4 쌍열 마운트를 추가했다.
  S5 TUR-558D는 S4 단장·쌍열과 S3 쌍열·3연장 구성을 지원한다.
- **HPS S4 220mm Hyper-Output Railgun.** Helix Propulsion Systems의 초고출력 단발
  전자기 가속포를 추가했다. 220mm는 발사 패키지/가속계 규격이며 약 60×700mm,
  35kg 장침 관통체를 6.2km/s로 가속한다. S4 마운트 포탑과 함체 고정식 설치를 지원하며,
  발사 후 냉각→재기동→펄스뱅크 충전을 순차적으로 거친다.
- **HPS 220mm Hyper-Velocity Penetrator.** 발당 4.25U, 전체 저장 질량 48kg의 전용
  운동에너지 탄약을 추가했다.
- **VHI HG-132/3.** Valken Heavy Industries의 S7 함체 고정식 132mm 3포신 중량
  개틀링을 추가했다. 회전체가 단계적으로 가속되어 최고 1,850RPM에 도달하며 별도
  Full-Bore AP 탄약을 사용한다.
- **VHI 132mm Full-Bore AP.** 발당 5.5U, 저장 질량 85kg, 40kg 관통체와 1.9km/s
  기준 탄속을 갖는 HG-132/3 전용 탄약을 추가했다.
- **MGI 제품 코드/분류.** 기존 MGI 표준 제품군에 모델 코드와 `C / M / I / S`
  분류를 고정하고, 신규 HPS/VHI 특화 제품은 각 제조사와 군용 분류를 명시적으로 가진다.
- **EO Stage 5 BDA LOCK.** EO/LRF 식별 단계에 정밀 전투 분석을 추가했다. 일반 BDA
  문턱과 별도로 LRF가 4km 이내를 확정하면 완화된 근거리 해상도·품질 문턱을 사용한다.
- **관측 기반 BDA 기록.** 외부 모듈의 관측 스냅샷, 보호막 상태 버킷과 최근 피격 흔적을
  트랙에 기록한다. 실제 적 모듈 HP를 UI에 직접 노출하는 방식은 사용하지 않는다.
- **락온 표적 전술도 복원.** 초기 1.21 계열의 표적 전술도 레이아웃을 현재 센서/BDA
  계약으로 다시 만들었다. 관측 가능한 범위 안에서 표적 방향, 거리, 구조 상태,
  보호막, 외부 모듈 BDA와 최근 피격 평가를 한 패널에 표시한다.

### 변경

- **고정식 무장 자세제어 우선순위**를 범용 AI 코어에 포함했다. 정밀 고정무장 비중이
  높으면 ACQUIRE/SETTLE 중 병진을 잠시 억제하고, 정렬 중 불필요한 궤도기동을 줄이며,
  정지에 필요한 각거리까지 고려한 회전 제어를 사용한다.
- Hull Aim은 전체 AI 자세를 덮어쓰는 별도 조종기가 아니라 범용 코어가 선택한 자세에
  제한된 trim을 더하는 방식으로 바뀌었다.
- 포탑 카탈로그는 제품 모델과 설치 크기를 같은 항목으로 복제하지 않고 `TUR-558 Series /
  TUR-558D` 계보와 S4/S5 실제 프로필을 연결한다.
- 기업 제품 기본값은 계속 MGI를 사용하지만, HPS 220mm 계열과 VHI HG-132/3 계열은
  명시적인 비-MGI 예외 제품으로 Company DB와 제품 메타데이터에 연결된다.
- BDA 내부 모듈과 피격점은 전장 본화면에서 제거하고 **락온 표적 인디케이터에만** 그린다.
- BDA LOCK에서 구조 건전도는 정밀 HP 숫자 대신 10% 단위 관측값으로 양자화해 표시한다.

### 수정

- **락온 정보가 단순 표식으로 축소되어 있던 UI 회귀**를 수정했다. 현재 센서 단계에 맞춰
  표적의 방향·함체·외부 모듈·BDA 정보를 단계적으로 복원한다.
- 정식 승격 과정에서 구현과 어긋나 있던 AI/BDA 회귀 하네스 3개의 비교식을 현재 런타임
  계약에 맞췄다. 실제 전투 로직 변경이 아니라 테스트가 올바른 입력·현재 렌더 경로를
  검사하도록 정리한 것이다.
- 정의되지 않은 과거 HPS WIP 테스트 alias를 제거해 실제 노출 하네스 수와 소스 선언을 맞췄다.

### 저장 호환

- `MODULE_STORAGE_SCHEMA_VERSION`은 **39**를 그대로 유지한다.
- 기존 1.31.34 설계는 별도 변환 없이 1.31.35에서 불러올 수 있다. 새 포탑 크기와 신규 무장도
  기존 `turretProfileId` / `mountProfileId` / `weaponProfileId` 저장 계약을 사용한다.
- 반대로 1.31.35에서 S5 TUR-558D, HPS/VHI 신규 무장처럼 1.31.34에 존재하지 않던 프로필을
  사용한 설계를 구버전으로 다시 여는 것은 호환 대상으로 보장하지 않는다.

### 검증

- JavaScript syntax **PASS**.
- 정식 파일 표시를 `SPACEFRAME GRID PROTOTYPE 1.31.35` / `PROTO 1.31.35`로 확인했다.
- 신규 핵심 회귀 **122/122 PASS**: AI Core 17/17, EO/BDA 9/9, HPS Railgun 13/13,
  Product Naming 32/32, Target Lock 12/12, Turret Placement 4/4, Turret Series 18/18,
  VHI HG-132 11/11, Company DB 6/6.
- 기존 영향권 회귀 **260/260 PASS**: Product Metadata 112/112, Price Integration 9/9,
  Ship Cost 4/4, Registry 83/83, System Adapter 37/37, Catalog Readability 4/4, UI-I1 11/11.
- 정식 승격 대상 신규/영향권 검증 합계 **382/382 PASS**.
- 위 검증 실행에서 페이지 오류 **0**, 콘솔 오류 **0**.
- 페이지에는 `window.__SPACEFRAME_*_TEST__` 형식의 자체 테스트 하네스가 **81개** 노출된다.

### 알려진 제한

- Universal AI Core는 현재 **전투 비행제어의 범용화**가 중심이며, TRACK이 불충분하거나
  코어 명령을 만들 수 없는 상황에서는 Legacy 전투 기동 fallback이 계속 존재한다.
- S2~S10 포탑 크기 표준은 정의되어 있지만 실제 `TUR-558D` 제품은 현재 S4/S5만 구현되어 있다.
- 기업 고유 무장은 이번 버전에서 HPS와 VHI가 처음 추가되었다. ADS/NCM 등의 고유 제품군은
  아직 별도 성능 프로필로 확장되지 않았다.
- BDA는 센서 관측 결과를 기억한 스냅샷이며 적 함선의 실제 내부 상태를 완전하게 공개하지 않는다.

---

## [1.31.34] — 2026-08-09

기준: v1.31.33 · tag `v1.31.34` · 실행 파일 `Spaceframe_v1.31.34.html`

1.31.33에서 전력·기동·가격·설계 보관함을 정리한 뒤, 이번 버전에서는 **게임 안에서
세계관과 기업 정보를 직접 조회할 수 있는 WORLD / CORPORATE DATABASE를 추가하고,
제품 메타데이터의 제조사를 실제 기업 레지스트리와 연결했다.** 현재 기본 장비군은
Meridian General Industries(MGI)의 범용 기준제품으로 정리했으며, 카탈로그와 설치된
모듈 정보에서 제조사를 눌러 해당 기업 페이지로 바로 이동할 수 있다.

### 추가

- **WORLD / CORPORATE DATABASE.** 설계 화면 상단에 `DB` 버튼을 추가했다. WORLD 페이지에서
  현재 연도 2592 CE, 워프·점프 게이트 기반 이동, 공학·산업 기준, 핵심 연표와 문명 규모
  공동 프로젝트를 열람할 수 있다.
- **5개 기업 레지스트리.** MGI(Meridian General Industries), ADS(Aegis Dynamic Systems),
  HPS(Helix Propulsion Systems), NCM(Novacore Materials), VHI(Valken Heavy Industries)의
  설립·본사·규모·기원·성장사·기업문화·강점·약점·기업 관계·역사적 프로젝트 역할을
  게임 데이터로 추가했다.
- **기업 로고 내장.** 다섯 기업의 로고를 WebP data URI로 단일 HTML 안에 포함했다. 별도
  이미지 파일이나 외부 요청 없이 기업 DB에서 표시된다.
- **기업 DB 연결 링크.** 제품 카탈로그의 `제작 기업`과 설치된 모듈 정보의 제조사 표시를
  클릭 가능한 기업 링크로 바꿨다. 해당 기업의 DB 페이지를 즉시 열 수 있다.
- **Company DB 회귀 하네스.** `__SPACEFRAME_COMPANY_DB_13134_TEST__`를 추가해 5개 기업
  등록, 2592 CE 세계 DB, 5개 로고, 기본 완제품 MGI 연결, 원재료 제조사 비강제, DB DOM을
  검증한다.

### 변경

- **기본 완제품 제조사를 MGI로 통일했다.** 모듈·RCS·레이더·보호막·열관리·열배관·포탑·
  마운트·무장·탄약 등 현재 완성된 기본 제품 레지스트리는 `manufacturerId: 'MGI'`를
  기본값으로 상속한다.
- `productizeRegistry()`가 공통 메타데이터 기본값을 받을 수 있게 확장되었고, 완제품용
  `productizeEquipmentRegistry()`를 추가해 같은 제조사 값을 각 제품에 반복해서 적지 않게 했다.
- 범용 탄약고와 범용 캐패시터의 기존 `manufacturerId: 'Meridian'` 표기를 정식 기업 ID인
  `MGI`로 정리했다.
- 재질과 라이너는 제조사를 임의로 붙이지 않고 계속 `manufacturerId: null`을 유지한다.
- 카탈로그 메타데이터의 제조사 표시는 단순 문자열 대신 기업 레지스트리 링크를 사용한다.
- 설치 모듈 요약에도 제조사 메타데이터를 노출해 탄약고·캐패시터·고정무장·포탑과 일반
  모듈에서 동일한 기업 연결을 사용할 수 있게 했다.

### 저장 호환

- 설계 저장 형식과 인스턴스 저장 데이터는 변경하지 않았다. 제조사·기업 정보는 제품
  레지스트리에서 조회하며 개별 설계 인스턴스에 중복 저장하지 않는다.
- 기존 1.31.33 설계는 별도 변환 없이 그대로 불러올 수 있다.

### 검증

- JavaScript syntax **PASS**.
- 정식 파일 표시를 `SPACEFRAME GRID PROTOTYPE 1.31.34` / `PROTO 1.31.34`로 확인했다.
- 신규/영향권 회귀 **241/241 PASS**: Company DB 6/6, Product Metadata 87/87,
  Price Integration 9/9, Ship Cost 4/4, Registry 83/83, System Adapter 37/37,
  Catalog Readability 4/4, UI-I1 11/11.
- 페이지 오류 **0**, 콘솔 오류 **0**.
- 기업 DB의 MGI/ADS/HPS/NCM/VHI 로고 **5/5 실제 렌더링**을 확인했다.
- 페이지에는 `window.__SPACEFRAME_*_TEST__` 형식의 자체 테스트 하네스가 **71개** 노출된다.

### 알려진 제한

- 이번 버전은 기업 세계관과 제조사 연결의 첫 통합이다. 현재 기본 완제품은 모두 MGI 범용
  제품으로 취급하며, ADS/HPS/NCM/VHI 고유 제품군과 기업별 성능·가격 차별화는 아직 추가하지 않았다.
- WORLD DB에 명시된 일부 설정은 문서 안에서도 미정으로 표시되어 있으며, 점프 게이트의
  세부 물리·법 체계나 인류권의 정확한 규모 등은 향후 확정 대상이다.

---

## [1.31.33] — 2026-08-09

기준: v1.31.32 · tag `v1.31.33` · 실행 파일 `Spaceframe_v1.31.33.html`

1.31.32에서 물리 무장과 방향성 센서를 설계 형상에 연결한 뒤, 이번 버전에서는
**함선의 전력 저장·충방전 처리량, 기동 추력기 배치, 제품 가격과 설계 보관함을 같은
설계 데이터 계약 안으로 묶었다.** 주엔진 하나가 모든 방향 기동을 대신하던 구조를
걷어내고 실제 배치된 RCS가 후진·횡이동·선회를 만들며, 반응로 내장 버퍼와 별도
캐패시터가 에너지 무장의 연사율까지 제한한다. 설계 화면에는 이 결과를 바로 읽을 수
있는 전력 Fitting과 함선 비용 표시를 추가했다.

### 추가

- **RCS 기동 추력기 제품.** `S2 RCS`(0.60MN, 0.5×0.75m)와 `S3 RCS`
  (1.20MN, 0.75×1m)를 독립 모듈로 추가했다. 외벽에서 1m 미만이면 가장 가까운
  외벽의 외향 법선으로 배기하고 그 반대 방향으로 추력을 만들며, 1m 이상 내부에
  설치하면 선수 방향으로 배기해 후진 추력기로 동작한다.
- **실제 RCS 추력선/토크 배분.** 각 RCS의 고정 노즐 벡터와 함선 질량중심까지의
  모멘트 암을 사용해 병진·회전 명령을 배분한다. 같은 수의 추력기를 달아도 설치 위치와
  방향이 다르면 횡가속과 선회 토크가 달라진다. RCS 가동률은 실제 전력 소비와 방향성
  IR plume에도 연결된다.
- **가변 크기 캐패시터 뱅크.** `capacitor-general-purpose`를 추가했다. 1×1m부터
  12×12m까지 설치할 수 있고 면적당 **6.5GJ 저장 · 4.0t 질량 · 1.5GW 충전 ·
  1.5GW 방전 · 1.2MCr 비용**이 선형 증가한다. 현재 제품은 크기 등급 없이 하나의
  범용 저장계로 취급한다.
- **캐패시터 파괴 급방전.** 저장 에너지가 남아 있는 캐패시터가 파괴되면 충전량에
  비례해 에너지를 소모하면서 내부 폭발·열·화재 피해를 발생시키는 별도 파괴 경로를
  추가했다.
- **충·방전 처리량 기반 전력 버스.** 함선 전력 상태에 `chargeLimitMW`,
  `dischargeLimitMW`, 버퍼 방전 사용량과 에너지 무장 요구량을 추가했다. 연속 소비가
  발전량을 넘으면 버퍼가 방전 처리량 한도 안에서 보충하고, 남은 방전 처리량을
  에너지 무장들이 공유한다.
- **에너지 무장 처리량 제한.** Energy Repeater와 Pulse Cannon은 발사 에너지뿐 아니라
  충전 속도도 함선의 실제 방전 처리량에 묶인다. 요구량보다 처리량이 부족하면
  `energyWeaponThroughputFactor`만큼 충전/연사 주기가 길어진다. 예를 들어 1.30GW가
  필요한 Repeater에 650MW만 공급되면 이론 연사율은 50%가 된다.
- **통합 Power Fitting UI.** 설계 화면 우측 전력 패널에서 발전량, 연속 소비,
  반응로/캐패시터 저장량, 총 충전·방전 처리량, 에너지 무장 요구량과 이론 연사율을
  함께 표시한다. 보호막 또는 열관리 도구를 선택하면 별도 패널로 교체하지 않고 같은
  패널이 넓어지며 아래쪽 스크롤 흐름에 해당 상세정보가 이어진다.
- **제품 메타데이터 계약.** 레지스트리/프로파일에 제조사 ID, 분류, 특수 분류와
  `base/perKg/perM2` 비용을 담는 `product-metadata-v1` 계약을 추가했다. 배치된 개별
  모듈에는 상업 메타데이터를 중복 저장하지 않고 제품 프로파일을 통해 조회한다.
- **설계 가격 계산.** 함체·장갑·내장재·모듈·열배관·포탑·마운트·무장·탄약의 Cr 가격을
  제품/재질 레지스트리에서 읽어 `순수 건조비`와 현재 탄약을 포함한 `전투 준비가`를
  계산한다. 구조·장비·무장·탄약 분해를 함께 표시하고, 가격이 없는 항목은 0으로
  숨기지 않고 **최소 금액(≥) + 미가격 품목 수**로 표시한다.
- **IndexedDB 설계 보관함.** 이름을 붙여 저장하는 최대 20개 설계 슬롯을 단일
  localStorage 배열에서 IndexedDB의 메타데이터/페이로드 분리 저장으로 옮겼다.
  `CompressionStream`을 지원하고 압축 이득이 있으면 설계 페이로드를
  `gzip-json-v1`로 저장하며, 미지원 환경에서는 `json-v1` 또는 기존 localStorage
  보관함으로 fallback한다. 기존 localStorage 슬롯은 최초 접근 시 이전한다.

### 변경

- **주엔진 역할을 전진 추진으로 한정했다.** 기존 주엔진의 후진/횡이동 비율을 제거하고
  18MN 전진 추력과 **6% 짐벌 보조**만 남겼다. 후진·횡이동·주 선회는 실제 설치된
  RCS가 담당한다. 주엔진 최대 전력은 620MW에서 **520MW**, 대기 전력은 30MW에서
  **2MW**로 재조정했다.
- **반응로 내장 버퍼를 6.4GJ → 2.25GJ로 조정했다.** 발전량은 기존 **2.4GW**를
  유지하고, 내장 버퍼의 충전·방전 처리량도 2.4GW다. 대용량 저장과 높은 펄스 처리량은
  별도 캐패시터를 설치해 확보하는 구조로 분리했다.
- **전력 소비 체계를 재조정했다.** 내부 계산 단위는 계속 MW를 사용하되 UI는
  W/kW/MW/GW/TW를 자동 선택한다. EO/IR/ESM, Sensor Fusion, FCS, 레이더, 포탑,
  마운트, 탄약고, 열관리 장치와 에너지 무장까지 설계/런타임이 같은 프로파일 전력값을
  사용하며 ESM과 레이더도 전력 breakdown에 명시적으로 포함된다.
- **레이더 전력 소비가 RF 운용과 연결됐다.** 레이더의 전기 부하는 실제 RF duty와
  EMCON 상태를 따라가며, RF 송신출력 자체와 함선 전력망 소비량은 계속 별도 값으로
  유지한다.
- **보호막 전력·저장량 재조정.** S1/S2/S3의 기준 용량을 각각 1.8/4.0/8.4GJ,
  처리량을 180/300/500MJ, 재충전을 30/65/140MJ/s로 재설정하고 해당 재충전율에서
  필요한 최대 전력 부하를 계산한다.
- **에너지 무장 발사 에너지를 전력 버스 기준으로 정리했다.** Energy Repeater는
  발당 130MJ, Pulse Cannon은 400MJ를 사용하며, 발사체/빔 에너지와 무기 자체 열로
  빠지는 에너지의 합이 버스에서 꺼낸 에너지와 일치하도록 맞췄다.
- **모듈 질량 재조정.** 센서·융합/FCS·레이더·보호막·열관리·포탑/마운트·무장·탄약고의
  기본 질량을 새 제품 프로파일로 재정리했다. 반응로 150t, 주엔진 34t은 유지하면서
  설계 질량과 관성 계산이 새 프로파일 값을 직접 사용한다.
- **제품 가격을 레지스트리 단일 출처로 이동했다.** RCS, 주엔진/반응로, 센서/레이더,
  보호막, 열관리/배관, 포탑/마운트/무장, 탄약/탄약고와 함체·장갑·내장재 가격이
  개별 UI 상수가 아니라 제품/재질 메타데이터에 저장된다.
- **카탈로그 상세 UI를 문서 흐름으로 재구성했다.** 헤더/미리보기 → 제품 메타데이터 →
  주요 제원 → 추가 특성 → 운용 설명 순서를 실제 DOM 순서로 만들고 CSS `order`에
  의존하던 재배치를 제거했다. 툴바 전력 표기도 짧은 SI 형식으로 정리했다.
- 기본 설계에 RCS 기동 추력기 4기를 추가해 새 추진 계약을 바로 확인할 수 있게 했다.

### 수정

- **설계 슬롯 JSON 내보내기가 현재 편집 중인 함선을 내보내던 문제.** 이제 선택한
  보관함 슬롯의 실제 저장 페이로드를 읽어 내보낸다.
- **IR/RCS 설계 오버레이의 공용 clamp 누락 경로.** 공통 `clamp()`를 정의해 특정
  오버레이 계산에서 `ReferenceError`가 발생할 수 있던 경로를 막았다.
- **에너지 무장 발사 에너지 중복 차감 가능성.** 실제 발사 시 에너지는 `fireTurret`
  경로에서 한 번만 차감하도록 계약을 고정했다.
- **카탈로그 WIP 스타일 잔재.** 새 문서형 상세 UI에서 사용하지 않는 기존 설명 카드의
  배경/좌측 테두리 스타일이 남아 가독성 회귀와 충돌하던 부분을 정식 승격 전에 제거했다.

### 저장 호환

- 설계 저장 스키마를 **39**로 올렸다.
- RCS 모듈의 `rcsProfileId`와 캐패시터의 `capacitorProfileId`를 저장/불러오기 계약에
  추가했다.
- 기존 1.31.32 설계는 계속 불러올 수 있지만 **RCS를 자동으로 새로 삽입하지 않는다.**
  주엔진이 더 이상 기존의 후진·횡이동을 대신하지 않으므로, 구형 함선은 새 버전에서
  후진·횡이동·주 선회 성능을 확보하려면 RCS를 직접 배치해야 한다.
- 자동 저장과 되돌리기 기반은 계속 localStorage를 사용한다. 이름을 붙인 설계 보관함만
  IndexedDB + 압축 저장으로 이전하며, IndexedDB를 사용할 수 없는 환경에서는 기존
  localStorage 보관함을 유지한다.

### 검증

- JavaScript syntax **PASS**.
- 정식 `Spaceframe_v1.31.33.html` 표시를 `SPACEFRAME GRID PROTOTYPE 1.31.33` /
  `PROTO 1.31.33`으로 확인했다.
- 정식 후보에서 신규/영향권 회귀 **290/290 PASS**:
  Storage Codec 3/3, Product Metadata 87/87, Power Foundation 7/7,
  Power Balance 10/10, Mass Balance 8/8, Propulsion Geometry 12/12,
  Overlay Regression 3/3, Price Integration 9/9, Catalog Readability 4/4,
  Ship Cost 4/4, Capacitor WIP25 7/7, Capacitor Throughput/Fitting 5/5,
  Registry 83/83, System Adapter 37/37, UI-I1 11/11.
- 페이지에는 `window.__SPACEFRAME_*_TEST__` 형식의 자체 테스트 하네스가 **70개**
  노출되는 것을 확인했다.

### 알려진 제한

- 실제 RCS 제품은 현재 S2/S3 범용 기동 추력기 두 종류이며, 캐패시터 제품은 범용
  가변형 한 종류다. 제품 레지스트리는 다품종 확장을 받을 수 있는 구조지만 이번
  릴리스에서 추가 제품을 가정하지 않는다.

---

## [1.31.32] — 2026-08-08

기준: v1.31.31 · tag `v1.31.32` · 실행 파일 `Spaceframe_v1.31.32.html`

1.31.31의 레이더/FCS 기반 위에서 **무기를 실제 형상으로 물리화하고, 함체 고정식
무장과 선체 조준을 연결했으며, 방향성 센서와 미러 편집을 설계 도구 수준에서
재정리했다.** 이번 버전의 중심은 새 무기 수를 늘리는 것이 아니라, 포신·센서 방향·
대칭 편집처럼 설계 화면에서 보이는 형상이 실제 전투 계산과 같은 데이터를 쓰도록
맞추는 데 있다.

### 추가

- **S3 무기 4종 물리 형상 통합.** 150mm Cannon, 60mm Autocannon, Energy Repeater,
  High-output Pulse Cannon이 모두 동일한 Weapon Profile에서 본체/약실과 포신/방출부
  형상을 가진다. 포탑 장착과 함체 고정은 별도 무기가 아니라 같은 무기의 장착 방식
  차이다.
- **실제 물리 포구 기반 사격.** 발사체 생성점과 FCS 운동학이 포탑 중심이나 가상
  포신이 아니라 실제 무기 형상에서 계산한 muzzle을 사용한다. 쌍열 마운트는 두 개의
  독립 hardpoint와 실제 포구를 가지며 중심 간격은 0.75m다.
- **함체 고정식 무장.** 별도 포탑/마운트 없이 Weapon Profile을 선체에 직접 설치할 수
  있다. 본체는 함체 안에 있어야 하고 포신/방출부는 외부로 돌출할 수 있으며, 무기별
  `fineGimbalDeg`를 사용해 소각도 보정을 한다. 포구는 최대 0.5m까지 후퇴 설치할 수 있다.
- **FCS Hull Aim.** 고정식 무장이 유효 FCS 해를 가지면 비행제어에 선체 지향 요청을
  보내 무기 중심축을 현재 선행사격 `worldAngle`에 정렬한다. 선체 각도를 직접 바꾸지
  않고 기존 엔진의 각가속도/관성 제어를 그대로 사용한다. 수동 조종은 자동 Hull Aim보다
  우선한다.
- **돌출 무기 독립 충돌체.** 선체 밖으로 나온 포신/방출부와 외부 본체가 투사체의
  swept collision 후보가 되어 선체나 다른 모듈보다 먼저 피격될 수 있다. 무기 HP는
  하나로 유지하며 본체/약실 피격은 1.0×, 포신/방출부 피격은 0.45× 피해 계수를 쓴다.
- **방향성 센서 단일 boresight.** EO와 IR은 설치면 수에 따라 FoV를 여러 개 만들지 않고
  센서 하나당 하나의 중심축과 하나의 FoV만 가진다. 모서리 설치도 자동 다중 FoV가 아닌
  단일 지향 센서로 처리한다.
- **센서 방향 재설정 UI.** 설치된 방향성 센서의 정보탭에서 재설정 모드로 들어가
  화살표를 직접 드래그할 수 있다. 방향은 15° 단위로 스냅하며 하단 편집 패널에서 현재
  방향, FoV, 가시 범위, 차폐율, 설치 유효성을 실시간으로 확인한 뒤 확정한다.
- **센서 자동 방향.** 새 방향성 센서는 가장 가까운 외곽 장갑의 외향 법선을 기본
  boresight로 사용한다. 모서리처럼 두 인접 외곽면의 거리가 같은 경우 두 법선의
  중간 방향을 사용하고 15° 단위로 스냅한다. 가까운 외곽 장갑이 없으면 함체 외곽
  법선을 fallback으로 사용한다.
- **미러 편집 동기화.** 미러모드가 켜져 있고 반대편에 형상상 대응하는 객체가 있으면
  처음부터 미러로 설치한 객체가 아니어도 같은 편집을 적용한다. 모듈 이동/삭제,
  포탑 마운트·무장, 센서 지향각, 탄약고 적재, 장갑 코너 전이 설정 등이 대상이다.
  방향값은 단순 복사가 아니라 공간 반사를 사용하며, FCS 연결은 반대편 대응 FCS로
  매핑한다.

### 변경

- **무기 물리 크기 확정.** 150mm는 본체 0.50×1.00m + 포신 0.25×2.75m,
  60mm는 0.50×0.75m + 0.25×2.00m, Energy Repeater는 0.50×0.75m +
  0.25×1.75m, Pulse Cannon은 0.75×1.25m + 0.25×2.50m를 사용한다.
- **고정식 무장 RCS.** 선체 내부 본체는 추가 외부 RCS를 거의 만들지 않고 실제로
  노출된 포신/방출부만 외부 기여를 낸다. 포탑 장착 무기는 마운트/포탑 가림 근사로
  물리 무기 형상 RCS에 0.8× 노출 계수를 적용한다.
- **Hull Aim 목표 자세.** 초기 WIP의 "미세 짐벌 한계 안까지만 최소 회전" 방식에서,
  선택된 고정무기 중심축 자체가 현재 FCS 선행사격 방향과 일치하도록 바꿨다. 미세 짐벌은
  사격 가능 범위와 잔여 오차 보정에 사용하며 특정 ±2° 값을 제어 코드에 하드코딩하지
  않는다.
- **FCS/비행제어 순서.** 같은 프레임의 센서/FCS 해를 Hull Aim이 사용하도록 갱신 순서를
  `Sensor → FCS → Hull Aim/Flight Control`로 정리했다. 포의 로컬 목표각도 현재 선체각과
  FCS `worldAngle`에서 매 프레임 다시 계산한다.
- **IR 설치 footprint 완화.** IR 본체를 0.50×1.00m에서 **0.50×0.75m**로 줄여
  대각/각진 소형 선체 외곽에서 설치가 지나치게 막히는 문제를 완화했다. 센서 성능,
  질량, HP, 전력, FoV는 유지한다.
- 방향성 센서의 수동 확정값은 이후 함체/장갑 수정으로 자동 재계산하지 않는다.
  `sensorFacingDeg`가 없는 구형 설계만 불러올 때 한 번 자동 방향을 부여한다.

### 수정

- 고정식 무장의 Hull Aim이 미세 짐벌 가장자리에서 멈춰 시각적으로 표적을 제대로
  향하지 않는 것처럼 보이던 문제를 수정했다.
- 함체 고정무장을 미러 설치/편집할 때 위치만 반사되고 포신 방향이 올바르게 공간
  반사되지 않던 문제를 수정했다.
- 모서리 센서가 노출면 두 개를 이유로 센서 하나에서 FoV 두 개를 생성하던 동작을
  제거했다.
- 센서 방향 편집 중 물리적으로 함체 안쪽을 바라보는 방향은 확정할 수 없게 하고,
  지향 자체는 가능하지만 일부 FoV가 가려지는 경우는 차폐율을 표시한 채 허용한다.

### 저장 호환

- 설계 저장 스키마를 **37**로 올렸다.
- 구형 `sensorFacingDeg` 없는 방향성 센서는 로드 시 한 번 자동 boresight를 받는다.
- 구형 0.50×1.00m IR은 로드 시 방향을 유지한 새 0.50×0.75m footprint로 변환한다.
  0.25m 설계 스냅 때문에 중심을 정확히 0.125m 이동할 수 없는 경우 가장 가까운 스냅
  위치를 사용하며 중심 오차는 최대 0.125m다.

### 검증

- JavaScript syntax **PASS**.
- 정식 `Spaceframe_v1.31.32.html`에서 릴리스 핵심 회귀 **233/233 PASS**:
  IR Install 6/6, Sensor Facing 16/16, Mirror Edit 12/12, Weapon Physics WIP8 10/10,
  WIP9 6/6, EO/IR 10/10, Tactical Sensor 14/14, Sensor-FCS A1 12/12,
  Sensor-FCS B1 11/11, Registry 73/73, System Adapter 37/37,
  Truth Separation 15/15, UI-I1 11/11.
- 정식본 표시를 `SPACEFRAME GRID PROTOTYPE 1.31.32` / `PROTO 1.31.32`로 확인했다.
- 기존 WIP 하네스의 버전 문자열은 개발 단계 추적용이므로 정식 승격 시 변경하지 않았다.

### 남은 작업

- 고정식 무장의 선체 조준은 현재 하나의 대표 fixed gun을 선택해 처리한다. 향후 무장
  그룹/우선순위 UI가 생기면 여러 방향의 고정포가 있는 함선에서 사용자가 대표 무장을
  직접 선택하도록 확장할 수 있다.
- 방향성 센서의 15° 스냅과 boresight 계약은 EO/IR에 적용되어 있다. 향후 외피 고정
  레이더나 지향성 전자전 센서가 추가되면 같은 계약을 재사용할 수 있다.
- 실제 레이더 장비는 여전히 2면 회전식 PESA 하나이며 ECM/재밍 본체는 미구현 상태다.

---

## [1.31.31] — 2026-08-08

기준: v1.31.30 · tag `v1.31.31` · 실행 파일 `Spaceframe_v1.31.31.html`

1.31.28~1.31.30에서 만든 레이더를 **형상 RCS → 실제 S-band RF → 탐지/Pd →
SEARCH/TWS/STT → 센서 융합 → FCS 사격해**까지 하나의 연속된 체계로 고도화했다.
이번 버전은 새 레이더를 하나 더 얹는 작업이 아니라, 앞으로 회전식·통합 마스트·외피
고정형과 다른 주파수 대역을 추가해도 같은 물리 계층을 공유할 수 있도록 현재 PESA를
기준 장비로 완성한 버전이다.

### 추가

- **레이더 모듈 프로파일 통합.** 카탈로그·질량·HP·전력·런타임 스케줄러가 같은
  `RADAR_MODULE_PROFILES`를 읽는다. 현재 레이더는 `radar-s5-rotating-pesa` 프로파일의
  **2면 독립 회전식 PESA**이며 두 면은 독립 RF/스케줄러 채널을 갖고 공통 기계식
  페데스털만 공유한다. 레이더 프로파일 ID는 설계 저장에도 보존된다.
- **독립 2면 beam scheduler.** 정상 운용에서는 4초 주기의 360° 기계 회전 위에 각 면이
  ±60° 전자주사를 독립 수행한다. SEARCH 기본 dwell은 6ms이며 한 프레임 안에서도 여러
  dwell 전환을 처리한다. TWS가 한 면의 시간을 쓰는 동안 반대 면은 SEARCH를 계속한다.
- **실제 STT 마스트 운용.** STT에 들어가면 연속 360° 회전을 멈추고, 측정/트랙에서 얻은
  표적 방위를 중심으로 페데스털을 지향한다. 선택된 면은 ±14° 제한 섹터를 기계적으로
  왕복하면서 STT dwell을 유지하고, 중간 슬롯에서 국부 SEARCH/TWS도 수행한다. 반대 면은
  공통 페데스털이 허용하는 반대쪽 섹터를 독립 탐색하며, STT 해제 후 현재 자세에서
  정상 회전으로 복귀한다.
- **레이더 설치 계약 세 종류.** `elevated-rotating`, `integrated-mast`, `surface-fixed`를
  분리했다. 노출 회전식과 저피탐 통합 마스트는 함선 중앙에도 설치할 수 있고 자함
  구조물에 시야가 막히지 않는다. 외피 밀착 고정 배열은 외부 장갑/외피 가까이에 있어야
  하며, 실제 조사선이 자함 함체·격벽에 막히면 SEARCH/TWS/STT 후보에서 제외된다.
- **S-band RF 물리 프로파일.** 현재 PESA를 2.6~3.4GHz, 기준 3.0GHz로 정의했다.
  2.5×2.5m 개구, 개구효율 58%, 면당 RF peak 20MW, 수신 대역폭 24MHz,
  Noise Figure 4.5dB, System Loss 6.5dB를 프로파일에 넣었다. 함선 전력망 소비량과 실제
  RF 송신출력은 서로 다른 값으로 유지한다.
- **모드별 RF 운용.** SEARCH/TWS/STT가 각각 6/10.5/18ms dwell과 15/17/21% RF duty를
  사용하고, 각 모드의 범위 안에서 실제 주파수 도약을 한다. beam event에는 주파수,
  파장, peak/average RF, 안테나 이득, EIRP, dwell과 duty가 기록된다.
- **물리 레이더 방정식.** 송신 RF 출력, 안테나 이득, 파장, 방향별 표적 RCS, 왕복
  거리손실, 수신 대역폭·열잡음·Noise Figure·System Loss를 이용해 수신 SNR을 계산하고
  그 결과에서 Pd를 얻는다. 주파수가 달라지면 같은 개구에서도 실제 beam width와 이득이
  함께 변한다.
- **Radar–FCS 정밀 연동.** TWS/STT의 마지막 실제 dwell 시각과 레이더 track-file을
  Sensor Fusion/FCS가 직접 사용한다. 다음 dwell 사이에는 측정 이력에서 추정한
  속도·가속도로 트랙을 현재 시각까지 전파하고, 시간 경과만큼 uncertainty를 늘린다.

### 변경

- **RCS 기준을 S-band 3.0GHz로 명시했다.** 기존 방향별 RCS 값은 이제 막연한 generic
  radar signature가 아니라 **3.0GHz S-band equivalent RCS**다. RCS 질의 경로는
  주파수를 받을 수 있게 해 두었으며, 현재 S-band 내부에서는 동일한 보정값을 쓴다.
- **모서리 산란(Edge) 모델을 재작성했다.** 이전에는 모든 평면도 코너가 사실상 4m
  높이의 강한 산란원처럼 더해지고, 방향이 맞지 않아도 큰 floor가 남아 복잡한 함형일수록
  Edge가 RCS를 지배했다. 인접 facet 길이에서 유효 support를 만들고, 방향 floor와 오목
  코너 증폭을 줄였으며, 여러 edge의 기여를 sublinear하게 합성한다. Edge는 없어지지
  않지만 특정 정렬에서만 보조적인 glint로 두드러진다.
- **외부 모듈 RCS가 레이더 프로파일을 읽는다.** 현재 회전식 PESA는 노출형 외부 모듈로
  계속 RCS에 잡히며, 향후 저피탐 통합 마스트와 다른 레이더가 각자의 `signature`
  프로파일을 가질 수 있는 구조가 됐다.
- **추상 `searchBoost / trackBoost / sttBoost`를 탐지식에서 제거했다.** SEARCH/TWS/STT의
  성능 차이는 임의 배율이 아니라 dwell, RF duty, 주파수, 실제 수신 SNR에서 나온다.
  NCTR 거리 게이트와 전술맵의 레이더 탐지 봉투도 같은 RF/SNR 계산을 사용한다.
- **ESM이 레이더의 실제 RF 메타데이터를 받는다.** 레이더 ON/OFF만 보는 대신 현재
  GHz, 운용 모드, RF 출력과 EIRP가 방출 정보에 실린다. 아직 방출원 모델명을 자동으로
  식별하지는 않지만, 향후 주파수·주사·STT 패턴으로 emitter fingerprint를 만드는 데
  필요한 관측값은 남는다.
- **FCS 오차 전파를 축별로 분리했다.** 레이더 거리 오차를 그대로 횡방향 탄착오차로
  취급하지 않고, 방위 오차는 직접 횡오차로, 거리 오차는 비행시간/선행량 오차로,
  속도·가속도 오차는 비행 중 운동예측 오차로 전파한다. STT에 임의 명중률 보너스를
  주지 않고 센서 track uncertainty가 좋아진 만큼만 사격해가 좋아진다.
- RCS/CS 시각화의 설명 패널이 세로 공간을 넘으면 내부 스크롤을 사용한다.

### 수정

- **Edge가 스텔스 형상의 저점을 강제로 떠받치던 문제.** STL-1 시제함에서 기존 Edge는
  20~30m²를 거의 전방위 바닥값처럼 만들었고 72방위 중 41방위에서 다른 RCS 성분의
  합보다 컸다. 새 모델에서는 해당 지배 방향이 0/72가 됐으며 평균 Edge 비중은
  약 52.3% → 6.0%로 낮아졌다.
- **레이더 프로파일 필수 필드가 기본 경계 센서 생성과 충돌하던 문제.** 수동으로 raw
  모듈을 만들던 경로도 프로파일 정규화와 기본 HP/footprint를 거치게 해, 앞으로 다른
  프로파일형 모듈에서도 같은 종류의 배치 실패가 나지 않게 했다.
- **beam 사이에서 정밀 레이더 트랙이 일반 coast로 떨어지던 문제.** 마지막 실제 radar
  dwell을 관측 시각으로 유지해 TWS/STT의 track-file이 다음 재방문까지 살아 있게 했다.
- **FCS 관측 지연이 EO/IR 기준으로만 계산되던 문제.** 레이더 TWS/STT는 실제 마지막
  dwell 시각과 재방문 주기를 반영한다.
- **고정된 레이더 측정 bias를 매 dwell마다 새로운 독립 잡음처럼 속도 불확실성에
  누적하던 문제.** 지속 추적 중 공통 bias가 미분에서 상쇄되는 성질을 uncertainty
  모델에도 반영했다.

### 검증

- JavaScript syntax **PASS**.
- 레이더/RCS/FCS 관련 회귀 **337/337 PASS**:
  Radar Core 64/64, Radar Scheduler 12/12, Radar Install 9/9, Radar Physics 12/12,
  Radar UI 9/9, Radar-FCS 9/9, Truth Separation 15/15, FCS A1 12/12,
  FCS B1 11/11, Tactical Sensor 14/14, EO/IR 10/10, RCS Edge 5/5,
  Registry 73/73, REG-F 45/45, System Adapter 37/37.
- **STL-1 RCS 회귀.** 3.0GHz 기준 총 RCS는 정면 13.77m², 60° 56.59m²,
  105° 7.41m². SEARCH Pd 50% 기준 거리는 각각 약 111km, 158km, 95km로
  형상 방향성이 실제 탐지거리 차이로 이어진다.
- **RF 모드 실측.** 100m² 표적, 3.0GHz, 안테나 정면에서 Pd 50% 거리는
  SEARCH 약 182km, TWS 약 206km, STT 약 237km. 기존 추상 boost 없이 모드별
  RF 시간·에너지 차이만으로 순서가 유지된다.
- **Radar–FCS 실측.** EO/IR 거리해가 없는 5km 횡기동 표적을 STT로 약 1.6초 지속
  추적했을 때 FCS `predictedSigma`가 약 15.1m(1σ)까지 수렴했다. 동일 조건의
  거리 없는 방위-only 해는 약 1.3km로 벌어진다. 자동사격 문턱 자체는 바꾸지 않았다.
- Truth Separation을 유지한다. 정밀 레이더 트랙과 FCS는 표적의 실제
  `x/y/vx/vy/ax/ay`를 읽지 않고 측정·이력·도플러에서만 추정한다.

### 남은 작업

- 현재 실제 레이더 모듈은 2면 회전식 PESA 하나뿐이다. 통합 마스트와 외피 밀착
  고정 배열은 설치·RCS·시야 계약만 준비되어 있고 실제 장비 프로파일은 아직 없다.
- RCS 재질 계수는 3.0GHz S-band 기준 하나뿐이다. L/X-band 등 다른 대역이 추가될 때
  재질·Edge·모듈의 주파수 응답을 별도로 확장해야 한다.
- ESM은 레이더의 실제 RF 메타데이터를 받지만 emitter fingerprint를 누적해 모델명을
  자동 식별하는 로직은 아직 없다.
- ECM/재밍은 기존 이음매만 있고 동작하지 않는다. 사이드로브·편파·PRF·펄스압축 등
  더 세부적인 RF 파형 물리는 아직 모델링하지 않았다.
- IR 방위는 현재 EO처럼 정밀 역분산 융합에 참여하지 않는다.

---

## [1.31.30] — 2026-08-07

기준: v1.31.29 · tag `v1.31.30` · 실행 파일 `Spaceframe_v1.31.30.html`

레이더와 EO를 축별 강점으로 융합하고, **추정이 관측에서만 나오도록** 남아 있던
참값 유출 경로를 전부 끊었다. 센서 코드는 표적의 실제 상태를 *측정*할 수는 있어도
*읽을* 수는 없다 — 이 둘은 겉보기에 비슷한 값을 만들기 때문에 실수로 참값을 직접
집어넣어도 눈에 잘 띄지 않는다. 그 차이를 드러내는 회귀 하네스를 함께 넣었다.

### 추가

- **레이더 거리 + EO 방위 융합.** 두 센서의 강점이 정확히 직교한다 — 레이더는 거리를
  잘 재고(방위는 빔폭에 묶인다), EO는 방위를 잘 잰다(거리는 못 만든다). 두 방위
  측정을 역분산으로 결합한다. 실측에서 레이더 단독 5.58mrad → 융합 3.28mrad.
- **측정 기반 속도·가속도 추정.** 측정된 극좌표를 월드 좌표로 되돌려 이력에 쌓고,
  그 이력을 최소제곱으로 미분해 속도를 얻는다. **도플러는 시선방향 강한 제약으로
  쓴다** — 좌표 미분은 방위 오차 × 거리 때문에 횡방향이 거칠고 도플러는 시선방향만
  정확해서, 두 측정의 강점이 서로를 정확히 메운다. 가속도는 속도 추정의 시간 변화에서
  얻는다.
- **`__SPACEFRAME_TRUTH_SEPARATION_TEST__`** — 참값 유출 회귀 하네스.

### 변경

- **회전 어레이를 마스트로 명시했다.** 함체 위에 솟아 있으므로 선체나 장갑이 시야를
  가리지 않는다. 배치 위치가 시야를 정하지 않고 오직 회전이 정한다 — 함 한가운데
  깊숙이 묻어도 360°가 나온다. 대신 그렇게 솟아 있기 때문에 자기 RCS가 가장 크다는
  기존 설정(`radarExposureClass:'elevated'`)이 이제 앞뒤가 맞는다.
- 레이더 정밀 경로가 `radarFix && !lrfLock`에서 동작한다(이전에는 EO가 함께 보이면
  통째로 우회됐다). EO는 방위 결합으로 참여한다.

### 수정 — 참값 유출 세 곳

- **트랙이 실제 표적 좌표에서 태어나던 문제.** `createSensorTrack`이 `target.x/y`에
  잡음을 얹어 시드하고 초기 불확실성도 참 거리에서 뽑았다. 첫 관측이 들어오기도 전에
  추정값이 이미 정답 근처에 있었다는 뜻이다. 이제 관측자 위치에 '아무것도 모름' 크기의
  불확실성으로 태어나고, 같은 틱의 첫 갱신이 실제 측정으로 채운다.
- **레이더만 단 함선이 coast 분기로 빠지던 문제.** `det`/`seen` 계산에 레이더 탐지가
  빠져 있어 레이더 단독 접촉은 "안 보이는 것"으로 취급됐다. 이전 버전에서 드러나지
  않은 이유는 시험 함선이 ESM을 함께 달고 있어 `seen`이 ESM으로 켜졌기 때문이다 —
  즉 **레이더 위치 경로가 한 번도 제대로 실행된 적이 없었다.** 레이더 관측을 `det`
  계산보다 먼저 구하도록 순서를 바꿨다.
- **속도·가속도를 표적에서 그대로 읽던 문제.** 레이더 경로가 `measurement.vx/vy/ax/ay`
  (참값 유래)를 썼다. 위 측정 기반 추정으로 대체했다.

### 검증

- 자체 테스트 하네스 **802 단언 통과 / 0 실패 / pageerror 0** (기준선 788).
- **회귀 하네스를 변이 시험으로 검증했다.** 고친 버그 셋을 하나씩 되돌린 사본을 만들어
  하네스가 실제로 잡아내는지 확인했다 — 통과만 하고 못 잡는 시험은 없는 것만 못하다.
  - 참값 시드 복원 → `track-is-born-at-the-observer-not-the-target`,
    `birth-uncertainty-does-not-encode-true-range`가 잡음
  - 속도를 참값에서 읽도록 복원 → `fused-track-velocity-also-follows-measurements`가 잡음
  - `seen`에서 레이더 제거 → `radar-only-contact-is-seen-not-coasting` 외 2건이 잡음
- 결정적 시험: 표적을 +y로 움직이면서 `target.vy`에는 −y를 적어 둔다. 관측자에서 볼 때
  순수 접선이라 도플러에 잡히지 않으므로, 횡방향 속도는 좌표 이력에서만 나올 수 있다.
  코드가 `target.vy`를 읽으면 추정 부호가 뒤집혀 즉시 드러난다.
- 속도 추정 실측(60km, 50초): 접근 −399 대 참값 −400, 횡단 (−4,399) 대 (0,400),
  대각 (−300,220) 대 (−300,220).
- 헤드리스 150초 실측(220km 시작, 1200m/s 접근, 횡기동): 220km IR만 → 190km EO+IR
  방위만 → **160km RDR+EO 융합**(위치 오차 234m, 속도 오차 25m/s, 사격 승인) →
  70km(위치 오차 83m, 속도 오차 **1m/s**). 렌더 포함 pageerror 0.

### 남은 작업

- IR도 방위 융합에 넣지 않았다(현재 EO만 참여).
- 레이더 종류가 회전식 PESA 하나뿐이다(기계식·AESA 미구현).
- ECM/재밍은 이음매만 있고 동작하지 않는다.
- 속도 추정 창(3.5초)이 고정이라 급기동에는 뒤처진다. 기동 감지에 따른 가변 창은 미구현.

---

## [1.31.29] — 2026-08-07

기준: v1.31.28 · tag `v1.31.29` · 실행 파일 `Spaceframe_v1.31.29.html`

1.31.28에서 만든 레이더는 엔진만 있고 화면도 사격통제 연결도 없었다. 이번에
그 셋을 붙여 **레이더가 실제로 플레이 가능한 상태**가 됐다. 더불어 전자전을
나중에 넣을 때 구조를 다시 뜯지 않도록 이음매만 미리 박아 두었다.

### 추가

- **레이더 스윕 표시.** 회전하는 두 면이 지금 어디를 보고 있는지를 전술맵에
  그대로 그린다. 사각이 돌아가는 것이 눈에 보여야 "지금 저쪽은 못 보고 있다"는
  판단이 성립한다. 탐지 반경 원은 표시용 상수가 아니라 레이더 방정식에서 역산해
  그리므로 그림과 모델이 어긋나지 않는다.
- **레이더 접촉 심볼.** 확립된 트랙에 거리·거리변화율(접근/이탈)·트랙 등급·품질을
  붙여 표시한다. 센서 패널의 `ACTIVE RADAR` 카드가 실제 모드(SEARCH/TWS/STT)와
  EMCON 상태를 표시하고, 접촉 목록에 `R#` 행이 추가된다.
- **긴 교전 봉투.** 출격 거리 상한을 50km → **250km**로 올리고 60/120/200km
  프리셋을 추가했다. 거리 단계 문구도 새 봉투에 맞게 다시 나눴다.
- **EW-S0 전자전 이음매(동작 없음).** `EW_CONFIG`, `EW_NONE`, `shipEwEmission`,
  `radarJammingState`, `radarJamRejection`, `radarBurnThroughRangeM`와 관측·트랙의
  재밍 필드를 미리 만들었다. **구현할 때 손댈 곳은 세 군데뿐이다** — 재머 모듈을
  등록하고, `radarJamRejection`에 실제 감쇠식을 넣고, `EW_CONFIG.enabled`를 켠다.

### 변경

- **확립된 레이더 트랙이 `rangeKnown=true`를 준다.** LRF에 이어 정밀 거리를 만드는
  두 번째 경로다. 다만 **탐지만으로는 넘어가지 않는다** — 트랙이 TWS/STT로 확립되고
  품질이 `fixTrackQuality`를 넘어야 한다. 실측 결과 세 구간이 뚜렷하게 갈린다:
  **~210km 탐지 · ~130km 확립 · 그 안쪽 사격해.**
- **도플러가 사격해 품질로 이어진다.** 레이더 단독 트랙의 속도 오차는 도플러
  가용도에 반비례한다. 접선 기동하는 표적은 탐지도 되고 거리도 잡히지만 속도해가
  서지 않아 **자동사격이 승인되지 않는다.** 우주에서 접선 기동이 남긴 유일한
  효과이자, 이번 도플러 설계의 실제 보상이다.
- 레이더 단독 접촉의 위치를 **레이더가 잰 극좌표(거리·방위)에서 되짚는다.** 실제
  좌표를 읽지 않으므로 측정 오차가 그대로 위치 오차가 된다.
- 트랙 `source` 표기에 레이더를 반영한다(`RADAR`, `RDR STT`, `RDR+EO` 등).

### 수정

- **200km 교전에서 적함이 화면 밖으로 나가던 문제.** `getCameraBase`의 `fitZoom`
  하한이 `.012`로 박혀 있어 약 107km까지만 담을 수 있었다. 출격 상한이 50km이던
  시절의 값이라 그때는 드러나지 않았다. 하한을 `TACTICAL_SENSOR_CONFIG.minCameraZoom`
  에 맞춰 250km 교전도 양쪽 함선이 함께 보인다.

### 내부 구조

- `tr.radarFix` — 사격통제로 넘길 수 있는 레이더 트랙인지를 한 곳에서 판정한다.
  `passiveBearingOnly`가 이 값을 보고 갈라지므로 기존 수동 센서 계약과 충돌하지 않는다.
- EO가 함께 보이는 접촉은 기존 EO 정밀 경로가 위치를 맡는다. 레이더 극좌표 경로는
  레이더 단독일 때 쓰인다 — 거리는 레이더가, 방위는 EO가 맡는 본격적인 융합은
  아직 하지 않았다(남은 작업 참조).
- **미구현 재밍 식을 미리 넣지 않았다.** 검증하지 않은 식을 게이트 뒤에 숨겨 두면
  켜는 순간 조용히 틀린다. 계약과 호출부만 두고 본체는 비워, 오늘의 계산이
  재밍 개념이 없던 때와 **비트 단위로 동일**함을 테스트로 고정했다.

### 검증

- 자체 테스트 하네스 **788 단언 통과 / 0 실패 / pageerror 0** (기준선 773).
- `__SPACEFRAME_RADAR_TEST__` 확장 — rangeKnown 관문, 미확립 접촉의 사격 차단,
  도플러가 사격해에 미치는 영향, 융합 위치가 레이더 측정에서 나오는지, EW 이음매의
  항등성(비트 단위 동일).
- `__SPACEFRAME_TACTICAL_SENSOR_TEST__` 확장 — 출격 상한이 레이더 사거리를 담는지,
  250km 교전에서 두 함선이 화면에 들어오는지, 스윕과 접촉이 실제로 픽셀을 그리는지.
- 확립 문턱은 실측으로 잡았다(정지 표적 3회 반복): ≤130km 확립, 150~210km 탐지되나
  미확립, 230km 미탐지. 시험 거리 180km는 그 중간의 안정 구간이다.
- 헤드리스 전투 루프 150초 실측(200km 시작, 1300m/s 접근, 횡기동): 168km에서
  확립 → `rangeKnown=true` → 자동사격 승인, 거리 오차 -47m, 거리변화율 -1298m/s.
  전 구간 두 함선이 화면 안에 유지되고 전술 화면이 활성. 렌더 포함 pageerror 0.

### 남은 작업

- **레이더 거리 + EO 방위의 본격적 융합이 없다.** EO가 함께 보이면 기존 EO 경로가
  위치를 맡는다. 각 센서의 축별 강점을 섞는 것은 다음 작업이다.
- 레이더 종류가 회전식 PESA 하나뿐이다(기계식·AESA 미구현).
- ECM/재밍은 이음매만 있고 동작하지 않는다. 채프·기만·사이드로브도 미구현.
- 레이더 거리 오차가 트랙마다 고정된 편향이라 시간에 따라 변하지 않는다.

---

## [1.31.28] — 2026-08-07

기준: v1.31.27 · tag `v1.31.28` · 실행 파일 `Spaceframe_v1.31.28.html`

**능동 레이더가 들어왔다.** 별도로 개발하던 항공 레이더 시뮬레이터
(`Radar Sim v7.31`)에서 순수 레이더 모델만 뜯어 이식했다. 지금까지 이 게임의
센서는 전부 수동이라 "켜면 보이지만 나도 드러난다"는 판단이 성립하지 않았다.
이제 레이더가 그 거래를 만든다 — 그리고 그 대가를 받아내는 쪽은 1.31.26에서
만든 ESM이다.

이번 버전의 범위는 **센서 코어까지**다. 전술맵 표시와 FCS 연동은 다음 버전이며,
따라서 **레이더 접촉은 아직 화면에 그려지지 않는다.**

### 결정적 전제 — RCS는 이미 있었다

`radarCrossSectionM2(observer,target)`가 방향별 RCS를 m²로 이미 돌려주고 있었다.
그 뒤에는 128방향 샘플, 정반사 로브, 모서리 회절, 모듈 기여, 장갑 재질 반사율
(`RADAR_MATERIAL_FACTORS`), 손상 반영까지 완성돼 있었고 **소비자만 없었다.**
설계 화면 범례에 *"RCS는 아직 레이더 탐지에 연결되지 않음"*이라 적혀 있었고
`__SPACEFRAME_SIGNATURE_TEST__`는 `radarIntegrated:false`를 돌려주고 있었다.
이번 작업은 RCS를 새로 만드는 일이 아니라 **켜져 있던 절반에 나머지 절반을
붙이는 일**이었다. 그 플래그는 이제 `true`다.

기본 함선 실측 RCS는 자세에 따라 **50.8 ~ 828.8 m²**로 16배 흔들린다.

### 추가

- **레이더 방정식.** `snr = (rcs/refRcs)·(refRange/R)⁴·boost`, `Pd = snr/(snr+1)`.
  왕복 전파라 R⁴에 반비례하는 이 한 줄이 레이더가 수동 센서와 다른 이유 전부다.
  기준을 100m²/60km로 잡아 탐색 모드에서 **통상 자세 약 200km**가 나온다.
  함선 객체를 모르는 순수 함수로 두어 나중에 미사일 시커가 그대로 재사용한다.
- **2면 회전식 PESA (`radar` 모듈).** 배면 두 면이 공통 축으로 회전하고 각 면은
  정면 기준 ±60°를 전자식으로 조향한다. 순간 커버리지 **240°/360°**, 남은 60°
  사각 두 개는 회전이 쓸고 지나가며 **재방문은 회전주기의 절반(2.0초)**이다.
  송신원이 하나라 AESA처럼 동시 다중 빔은 못 세운다.
- **능동 센서 capability(`activeSensor`).** 이 게임 최초의 능동 센서다. 회전하는
  대형 어레이라 외피 위로 솟아야 하고, 그래서 노출 등급이 `elevated`이며 자기
  RCS 계수가 가장 크다 — 멀리 보는 대가로 자신이 잘 보인다는 거래가 모듈 정의
  단계에서부터 성립한다.
- **레이더 EM 대역(`radar`).** 다른 대역이 새어나가는 신호인 것과 달리 이쪽은
  의도적 방사라 압도적으로 크고, 운용 모드(탐색/TWS/STT)에 따라 방출이 달라진다.
  `ESM_POWER_PRIORS`에도 항목을 넣었다 — 1.31.26에서 *"새 방출원(레이더 등)은
  표에 항목을 추가하기만 하면 된다"*고 설계해 둔 확장 지점을 그대로 썼다.
- **EMCON 스위치(`systems.radarEmitting`).** 보호막의 `shieldOnline`과 같은
  자리에 둔다. 끄면 방출도 탐지도 즉시 완전히 멈춘다.
- NCTR 점진 분류, 트랙 coast/TTL, 미사일용 `radarIlluminated`와
  `datalink.maxArh`/`maxSarh` 선언(이번 버전에서는 소비하지 않는다).

### 변경

- **수동 센서 탐지 거리를 함께 올렸다.** 레이더가 150~200km에서 잡기 시작하므로
  기존 계열이 상대적으로 무의미해지지 않도록 재보정했다.
  `emRangeScaleM` 12→40km, `irRangeScaleM` 16→50km, ESM 불확실성 영역 상한
  22→75km. **EO/LRF(18km)는 그대로 뒀다** — "탐지는 길고 사격은 짧다"가 이 판의
  골격이 된다.
- 도플러는 **탐지를 막지 않는다.** 우주에는 지면 클러터가 없어 원본의 노칭
  (접선 기동으로 도플러 회피)은 물리적 근거가 없다. 대신 시선방향 속도가 0이면
  도플러 정보 자체가 없다는 사실만 남겨, 거리변화율을 아예 공표하지 않고 트랙
  품질을 떨어뜨린다. 접선 기동은 여전히 의미가 있지만 숨는 수단은 아니다.
- 버렸다: 고도·3D, 지면 클러터(`lookDownPenalty`, `lowAltClutterFloor`), 대기
  모델, 채프, 항공기 표적 레지스트리. 우주 배경에서 근거가 없다.

### 수정

- **회전식 레이더가 접촉을 영원히 확립하지 못하던 문제.** 처음에는 탐지 품질을
  느린 적분기로 쌓았는데, 회전식은 재방문 주기만큼 빔이 필연적으로 비우므로
  감쇠가 공백을 이겨 품질이 0.12에서 매번 무너졌다. dwell 한 번을 탐지 기회
  한 번으로 보고 "적어도 한 번 잡을 확률"로 누적하도록 바꿨다. 강한 반사는 빔이
  한 번 지나가면 바로 잡히고, 약한 반사는 몇 바퀴를 돌아도 확립되지 않는다.
- 트랙·탐지 품질의 감쇠 시간축을 재방문 주기가 아니라 TTL에 맞췄다.

### 내부 구조

- 신규: `radarSnrLinear`, `radarDetectionPd`, `radarDetectionRangeM`,
  `radarRangeRateMps`, `radarDopplerQuality`, `radarFaceBearings`,
  `radarFaceCoverage`, `radarInstantaneousCoverageFrac`, `radarRevisitPeriodS`,
  `radarModeDuty/Boost/RangeNoiseM`, `radarObservation`, `updateRadarTrack`,
  `radarEmissionMode`, `radarOperationalFactor`, `shipRadarProfile`.
- `RADAR_CONFIG`, `RADAR_PROFILES`, `ESM_POWER_PRIORS.radar`, `EM_BANDS.radar`.
- **`updateRadarTrack`은 표적 객체를 받지 않는다.** 실제 표적을 읽는 곳은
  `radarObservation` 하나뿐이고 읽는 즉시 측정 오차를 얹는다 — ESM과 같은 계약이다.
- **사격 계약은 건드리지 않았다.** 레이더가 정밀 거리를 만들어도 `rangeKnown`은
  false로 남고 결과는 `radar*` 전용 필드에만 실린다. ESM이 `esmAreaRanged`를
  `rangeKnown`과 분리했던 것과 같은 방식이며, FCS 연동은 다음 버전이다.

### 검증

- 자체 테스트 하네스 **773 단언 통과 / 0 실패 / pageerror 0** (기준선 697).
- 신규 하네스 `__SPACEFRAME_RADAR_TEST__` 52 단언 — R⁴ 법칙, Pd 곡선, 모드별
  처리 이득, 기존 RCS 소비, 자세·장갑 재질이 탐지거리를 바꿈, 2면 회전 기하
  (240° 순간 커버리지·사각·재방문·모든 방위가 1주기 안에 훑힘), 모듈 계약,
  런타임 탐지·측정·coast, 도플러(접선 기동에서 탐지 유지 + 품질 저하 + 거리변화율
  보류), EMCON, `rangeKnown=false` 유지.
- `__SPACEFRAME_SIGNATURE_TEST__`의 `radarIntegrated`가 **true**로 바뀌었다.
- 스케일 재보정으로 기존 하네스 8건이 깨져 전부 고쳤다. ESM 지오로케이션
  하네스는 시험 거리를 상수로 못 박는 대신 **EM 스케일에 묶어** 다음 재보정에서
  다시 깨지지 않게 했다.
- 그 과정에서 기존 단언 하나가 잡아낸 것: 측정 잡음을 꺼 두면 방위선이 전부
  참값을 지나므로 최소제곱이 진짜 좌표로 수렴하는 것이 **정상**이다. 해당 단언은
  잡음을 켠 상태에서만 의미가 있어 그렇게 고쳤다.
- 헤드리스 전투 루프 120초 실측(180km 시작, 횡기동 접근, 60초 락온, 90초 EMCON):
  167km에서 탐지 → STT에서 거리 오차 **2m** → NCTR 분류 완료 → EMCON 차단 시
  상대 ESM의 대역 목록에서 `RDR`이 사라짐. 전 구간 `rangeKnown=false`, 자동사격
  차단 유지. 렌더 경로 포함 pageerror 0.
- **EMCON 실측**: 레이더 OFF 방출 193MW → 상대 ESM 탐지 275km. 레이더 ON(탐색)
  470MW → **432km**. 레이더 자신의 탐지거리는 189km. 즉 **ESM이 레이더보다 2.3배
  멀리 듣는다** — 편도 대 왕복 전파의 물리가 그대로 게임 긴장이 된다.
- 성능: `radarObservation`+`updateRadarTrack` **3.12µs**/관측자·표적·틱,
  관측만 1.77µs, RCS 조회 0.46µs(기존 dirty-flag 캐시가 작동).

### 남은 작업

- **전술맵 표시와 FCS 연동이 없다.** 이번 버전의 레이더는 테스트로만 확인 가능한
  엔진이며, 다음 버전에서 화면과 사격통제에 연결해야 플레이 가능해진다.
- 레이더 종류가 회전식 PESA 하나뿐이다. 기계식·AESA는 이후 추가.
- 출격 거리 슬라이더 상한이 50km라 레이더 사거리와 맞지 않는다. 다음 버전에서
  전술맵 축척과 함께 조정한다.
- ECM/재밍, 버스루 거리, 사이드로브는 이식하지 않았다.

---

## [1.31.27] — 2026-08-07

기준: v1.31.26 · tag `v1.31.27` · 실행 파일 `Spaceframe_v1.31.27.html`

ESM 관측 계약에 남아 있던 참값 필드를 정리하고, 설계·전투 화면의 UI가 실제
작업 흐름을 더 잘 따르도록 다듬었다. 설계 화면의 모듈 정보창은 더 이상 브라우저
HUD처럼 화면 가장자리를 따라다니지 않고 선택한 모듈의 설계 좌표에 붙으며,
전투 무장 패널은 우측 함선 상태 패널 직전까지 확장되어 많은 무장을 한 번에
확인하고 스크롤할 수 있게 됐다.

### 추가

- **전투 무장 패널 가로 스크롤.** 무장 카드가 패널 너비를 넘으면 실제
  `overflow-x` 스크롤이 생기고, 스크롤바를 직접 사용할 수 있다.
- 터치 환경에서는 무장 목록을 좌우로 밀어 스크롤할 수 있고, 데스크톱에서는
  무장 목록 위의 마우스 휠/트랙패드 세로 입력도 가로 스크롤로 변환한다.
- 많은 무장을 배치한 상태를 확인하기 위한 overflow 스트레스 검증을 추가했다.

### 변경

- **설계 화면 모듈 정보창을 화면 UI가 아니라 설계 공간에 종속시켰다.**
  기존에는 정보창이 `body`에 `position:fixed`로 붙어 있어 설계를 팬·줌해도
  화면 가장자리를 따라 재배치되는 HUD처럼 동작했다. 이제 `buildScene` 내부의
  `position:absolute` 오버레이로 두고, 선택한 모듈의 설계 좌표를 화면 좌표로
  변환해 배치한다.
- 모듈 정보창의 좌우 배치는 현재 브라우저 여백이 아니라 **함선 설계상 좌우
  절반**을 기준으로 정한다. 팬·줌하면 정보창도 선택한 모듈과 함께 움직이고,
  모듈이 화면 밖으로 나가면 정보창도 함께 화면 밖으로 나간다.
- 화면 가장자리 클램프와 공간에 따라 정보창이 반대편으로 계속 튀는 동작을
  제거해, 선택한 모듈과 정보창의 공간적 관계가 유지되도록 했다.
- **전투 무장 패널의 가로 폭을 확대했다.** 패널은 화면 왼쪽에서 시작해 우측
  함선 상태/피격 인디케이터 바로 앞까지 자동으로 늘어나며, 두 UI 사이에는 작은
  고정 여백만 남긴다.
- 무장 카드가 적을 때 각 카드가 남는 공간을 과도하게 늘려 먹지 않도록 카드
  basis를 안정화했다.
- 센서 패널의 기존 `ESM 원` 표현을 현재 구현에 맞는
  **`ESM 불확실성 영역`** 표현으로 바꿨다.
- 설계 화면 프로토타입 표시와 시작 로그의 버전 문자열을 1.31.27로 통일했다.

### 수정

- **공용 `sensorObservation()`에 남아 있던 `emEmittedMW` 참값 필드를 제거했다.**
  v1.31.26부터 ESM 위치추정은 이 값을 더 이상 사용하지 않았지만, 관측 객체에는
  실제 표적의 총 송신출력 필드가 여전히 남아 있었다. 향후 레이더·전자전 작업에서
  잘못 재사용될 여지를 없애기 위해 관측 계약에서도 완전히 제거했다.
- 설계 화면의 모듈 정보창이 팬·줌 중 화면 가장자리로 따라오거나 반대편으로
  재배치되어 선택한 모듈과의 관계를 잃던 UI 동작을 수정했다.
- 전투 화면에서 무장 수가 많아질수록 하단 패널 안에서 카드들이 눌리거나 일부
  상태를 보기 어려워지던 문제를, 패널 확장과 실제 가로 스크롤로 해결했다.

### 내부 구조

- 모듈 정보창의 부모 컨테이너를 전역 `body`에서 `buildScene`으로 옮기고,
  화면 고정 좌표가 아니라 build canvas의 설계 좌표 변환 결과를 사용하도록
  배치 경로를 정리했다.
- 전투 무장 목록은 가로 overflow를 보존하는 구조로 바꾸고, 휠 이벤트를 가로
  스크롤로 전달하는 입력 경로를 추가했다.
- ESM geolocation, sensor fusion, FCS 계산식 자체는 변경하지 않았다.

### 검증

- JavaScript syntax **PASS**, pageerror **0**.
- `__SPACEFRAME_ESM_GEOLOCATION_TEST__` **63/63**.
- `__SPACEFRAME_ESM_BASELINE_TEST__` **22/22**.
- 전술 센서 화면 **10/10**, EO/IR **10/10**.
- SENSOR-FCS A1 **12/12**, SENSOR-FCS B1 **11/11**.
- EM 방출 **13/13**, ESM 모듈 **13/13**.
- 무장 회귀: H1 **17/17**, H2 **10/10**, P1 **10/10**, X1 **15/15**.
- Registry **68/68**, REG-C **58/58**, REG-D **59/59**,
  System Adapter **37/37**.
- 탄약 질량 **9/9**, 탄약고 **9/9**, UI-I1 **7/7**.
- 1440×900 브라우저 레이아웃 실측에서 무장 패널은 `x=0..896`,
  우측 함선 상태 패널은 `x=904..1430`으로 **8px 간격**을 유지했다.
- 무장 overflow 스트레스 검증에서 목록 표시폭 `887px`, 콘텐츠 폭 `1820px`,
  `overflow-x:auto`와 가로 스크롤이 정상 동작했다.

---

## [1.31.26] — 2026-08-07

기준: v1.31.25 · tag `v1.31.26` · 실행 파일 `Spaceframe_v1.31.26.html`

ESM 위치추정을 관측 기반으로 다시 세웠다. 실제 표적 상태와 ESM이 관측·추정한
상태를 구조적으로 분리하고, 세 종류의 증거가 서로 다른 축을 담당하게 만들었다.
결과적으로 ESM 모듈을 함선 어디에 다는지, 자함을 어떻게 기동하는지가 실제
위치추정 성능으로 이어진다.

### 추가

- **송신출력 prior (`ESM_POWER_PRIORS`).** 방출원 종류별 예상 송신출력 범위를
  선언하는 별도 계약. 새 방출원(레이더 등)은 표에 항목을 추가하기만 하면 되고,
  운용 모드가 있으면 같은 구조로 세분할 수 있다. `esmPowerPriorForBands`가
  식별된 대역들의 prior를 합치고, 식별 대역이 수신 전력의 일부만 설명하면 그
  비율로 되돌려 총 출력 범위를 세운다.
- **ESM 관측 이력 (`tr.esmObservations`).** 관측마다
  `{observerX, observerY, bearingRad, bearingSigmaRad, measuredAtS, samples}`를
  남긴다. 신호가 실제로 수신될 때만 쌓이며, 자함이 거의 움직이지 않았고 방위
  변화도 작으면 새 기록 대신 기존 관측에 병합해 σ를 √N으로 좁힌다(계통 성분이
  바닥이다). 나이 초과분은 버리고, 용량을 넘으면 가장 오래된 끝이 아니라
  기하적으로 가장 덜 기여하는 중간 관측을 솎아 기선을 지킨다.
- **방위 측정 오차 모델.** 수신기 고유의 계통 편향(관측마다 상관되어 평균해도
  지워지지 않는다)과 표본 잡음을 나눠 넣었다. 이 때문에 실제 방출원이 추정 영역
  밖에 놓이는 일이 자연스럽게 일어난다.
- TMA 해가 좌표 하나가 아니라 중심·장축 방향·major/minor·해 품질·관측 수·
  관측 시간폭·유효 기선을 함께 보고한다.

### 변경

- **신호세기 거리추정이 실제 송신출력을 더 이상 읽지 않는다.** 이전에는
  `obs.emEmittedMW`(표적의 참값)에 트랙 고유 편차를 곱해 썼다. 이제 분류 결과로
  세운 출력 prior와 수신 세기를 비교해 거리 **범위** `[Rmin, Rmax]`와 그 안의
  점추정을 만든다. 분류 신뢰도가 낮으면 prior가 배율로 넓어지고, 대역을 하나도
  짚지 못하면 가장 넓은 prior로 가 사실상 거리가 만들어지지 않는다.
- **함내 다중 ESM 기선의 주 효과를 방위 정밀도로 옮겼다.** 이전에는 기선이 거리
  해에 직접 들어갔다. 이제 σ_θ ≈ k/b_eff 로 방위를 좁히고, 그 결과가 타원의
  횡방향 폭(minor)을 정한다. 거리 방향은 신호세기와 TMA가 담당한다. 유효 기선이
  최대인 쌍을 자동 선택하고 손상 수신기를 제외하는 기존 특성은 그대로다.
- **TMA가 자함 이동 기반 합성 기선에서 실제 관측 이력 기반 가중 최소제곱으로
  바뀌었다.** 관측 i의 잔차는 방위선에 대한 수직 거리이고, 가중치는 `(σᵢ·rᵢ)⁻²`에
  관측 나이 반감기를 곱한 값이다. 관측 이후 표적이 움직였을 만큼을 방위 오차에
  더해 오래된 방위선을 덜 믿는다.
- **세 증거를 마지막 단계에서 정보행렬로 한 번만 합친다.** bearing evidence는
  시선에 수직인 방향만, signal evidence는 시선 방향만, TMA는 두 방향 모두를
  구속한다. 합친 정보행렬의 역이 위치 공분산이고, 이를 고유분해해 중심·장축
  방향·두 반축을 얻는다.
- **불확실성 영역이 실제 표적을 반드시 품지 않는다.** 이전에는 실제 표적 좌표를
  타원 국소 좌표로 옮겨 두 반축을 필요한 만큼 넓히는 "포괄성 보장"이 있었다.
  확률적 위치추정 영역이므로 이를 걷어냈고, 대신 1.66σ(2D 기준 약 75% 포함)
  타원을 그린다.
- 그려지는 쐐기의 폭도 실제 측정 방위 σ를 따른다. 기선이 좋으면 쐐기가 눈에 띄게
  가늘어지고 라벨 단위가 도에서 mrad로 바뀐다.
- 측거 근거 표시가 세 증거 구분을 그대로 반영한다(`신호세기 + TMA 4.6km + 기선 27m`).

### 수정

- **거리 정보가 사실상 없는 신호세기 추정이 횡방향 폭까지 부풀리던 문제.**
  상정 출력 범위가 너무 넓어 쓸 수 없는 거리 추정치라도 기준 거리로 쓰이면서,
  방위 오차가 엉뚱하게 큰 거리에 곱해져 minor가 함께 커졌다. 그런 증거는 아예
  세지 않고 명목 거리로 되돌린다.
- **관측을 쌓기만 해도 함내 기선의 이점이 사라지던 문제.** 최소제곱은 관측을 서로
  독립으로 보고 √N만큼 좁히지만 방위 오차의 계통 성분은 지워지지 않는다. 그
  성분을 횡방향 σ의 바닥으로 깔았다.
- **제자리에 있는 동안 매 틱 최소제곱이 다시 풀리던 문제.** 병합 관측이 이력
  버전을 올려 캐시가 한 번도 적중하지 않았다. 병합은 이력의 구성을 바꾸지 않으므로
  버전을 올리지 않고 시간 스로틀로 따라잡게 했다.

### 내부 구조

- `updateEsmUncertaintyArea`가 **표적 객체를 아예 받지 않는다**(`(tr, observer,
  obs, dt)`). 실제 좌표가 추정으로 새어들 경로가 구조적으로 존재하지 않는다.
- 신규 함수: `esmPowerPrior`, `esmPowerPriorForBands`, `esmBearingSigma`,
  `esmMeasuredBearing`, `esmGaussianUnit`, `recordEsmObservation`,
  `pruneEsmObservations`, `esmTmaInformation`, `esmTmaSolution`,
  `esmSolveInformation`, `esmCovarianceAxes`, `esmProjectVariance`,
  `esmCachedTma`, `esmFusePosition`.
- 제거된 함수: `esmRangeSolution`, `esmUncertaintyAxes`, `esmMotionBaseline`
  (이력 기반 유효 기선이 대신한다). 제거된 트랙 필드: `_esmPowerBias`,
  `_esmTmaAnchorX/Y/AtS`, `_esmAreaNoiseAngle`, `_esmAreaNoiseScale`.
  제거된 설정 키: `signalRelSigmaBest/Worst`, `tmaSampleTauS`,
  `tmaMaxBaselineM`, `esmAreaMinRadiusM`(미사용).
- 신규 트랙 필드: `esmObservations`, `esmMeasuredBearingRad`, `esmSignalRangeM`,
  `esmSignalSigmaM`, `esmPowerPriorMinMW/MaxMW`, `esmTmaQuality`, `esmTmaCount`,
  `esmTmaMajorSigmaM`, `esmTmaMinorSigmaM`, `esmTmaMajorAxisRad`.
- **센서 통합·FCS 계약은 건드리지 않았다.** ESM 단독 접촉은 계속
  `rangeKnown=false`이고, `esmAreaRanged`와는 의미가 분리되어 있다. 명목 거리는
  거리 정보가 없을 때의 표시 스케일로만 쓰인다. ESM-only 자동사격 하드 게이트도
  그대로다.

### 검증

- 자체 테스트 하네스 **693 단언 통과 / 0 실패 / pageerror 0** (기준선 644).
- 신규 하네스 `__SPACEFRAME_ESM_GEOLOCATION_TEST__` 63 단언 — prior 계약, 실제
  송신출력 없는 거리추정, 분류 신뢰도 반영, 관측 이력 축적·병합·상한·노후화,
  TMA 기하(횡기동 수렴 / 직선 접근 미수렴), 세 증거의 축 분담, 4단계 발전,
  신호 소실·재획득, 계약 유지.
- 갱신된 하네스 `__SPACEFRAME_ESM_BASELINE_TEST__` 22 단언 — 기선 기하와
  σ_θ ∝ 1/b_eff 관계, 손상 수신기 제외, 함선 회전, minor 축 반영.
- 추정 정확도는 통계로 판정한다(24회 앙상블: 실제 방출원 포함률 ≥60%, 평균 위치
  오차 < 표적 거리의 30%). 실제 좌표는 assertion의 기준값으로만 쓴다.
- 실제 전투 루프 실측(ESM 2기, EO/IR 없음, 18km 시작, 횡방향 사인 기동, 45초
  시점 적 EMCON): 최초 접촉 쐐기 22.0km → 15초 후 타원 major 5.6km / minor 0.42km,
  위치 오차 0.2~0.9km. EMCON 이후에도 접촉 유지, `rangeKnown=false`와 자동사격
  차단은 전 구간 유지. 7200틱 + 렌더 경로에서 pageerror 0.
- 성능(관측 이력 26건 만재 기준, `updateEsmUncertaintyArea` 순 비용):
  ESM 1기 3.8µs, 4기 1.9µs, 대기동 중 신규 관측이 매 틱 쌓이는 최악 경로 2.9µs.
  이전 구현은 0.8~1.3µs였으므로 약 2~3배이며, ESM 단독 접촉에만 드는 비용이다.

### 남은 작업

- 기본 설계(`defaultDesign`)에 ESM이 한 기도 없다. 기본 탑재 여부는 여전히 미정.
- TMA가 표적을 정지 상태로 가정한다. 표적 기동은 관측 나이에 따른 방위 오차 증가와
  거리 정밀도 18% 바닥으로만 반영된다. 표적 속도까지 추정하는 완전한 TMA는 미구현.
- 육안 확인은 전술맵 렌더 캡처와 헤드리스 전투 루프로 대신했다. 실제 출격 플레이
  확인은 하지 않았다.

---

## [1.31.25] — 2026-08-07

기준: v1.31.24 · tag `v1.31.25` · 실행 파일 `Spaceframe_v1.31.25.html`

ESM이 "이 원 안 어딘가"를 그리던 것을 걷어내고, 수동 측거 세 갈래를 합쳐 적의
대략적인 위치와 거리를 지속적으로 보여주도록 바꿨다.

### 추가

- **ESM 수동 측거 세 갈래.** 서로 독립인 세 경로를 역분산으로 합쳐 하나의 거리해를
  만든다. 하나만 좋아도 전체가 좋아진다.
  - *신호세기* — 방출원 종류로 송신 전력을 가정하고 전파식
    `received = P/(1+(R/scale)²)`를 역산한다. 수신기 1기로도 즉시 성립하는 유일한
    경로이며, 가정 오차가 그대로 거리 오차가 되어 거칠다(분류 확실 ±42%, 미분류는
    거리 미공표).
  - *안테나 기선* — 함체에 떨어뜨려 단 ESM 쌍의 위상 간섭계. 표적 방위에 수직인
    성분만 유효하다(`b_eff = |dx·sinθ − dy·cosθ|`). 모든 쌍을 훑어 유효 기선이
    최대인 쌍을 자동 선택하므로 설치 위치는 임의로 둘 수 있다.
  - *자함 기동(TMA)* — 접촉 이후 방위선을 가로지른 이동거리가 합성 기선이 된다.
    안테나 간격이 미터 단위인 데 반해 이쪽은 km까지 자란다. 안테나 쌍과 완전히
    같은 기하라서, 표적 방위로 곧장 달리면 아무것도 얻지 못한다.
- 측거 근거를 화면에 노출한다(`기선 16m + 기동 2.9km`, `신호세기`). 플레이어가
  배치와 기동의 효과를 즉시 읽을 수 있다.
- 방위 정밀도를 크기에 맞는 단위로 표시한다(진폭비교 DF는 도, 위상 간섭계는 mrad).

### 변경

- **ESM 불확실성 영역이 원에서 회전 타원으로 바뀌었다.** 시선 방향으로 길쭉하고,
  기선이 없으면 무한히 긴 쐐기로 수렴한다. 쐐기와 타원이 분기 없이 같은 공식의
  양 끝이다.
- **쐐기/타원 판정 기준을 `major/minor` 비에서 상대 거리오차로 바꿨다.** 기존
  비율 기준은 바닥이 걸린 `minor`와 `major`를 비교해서, 간섭계 방위가 정밀할수록
  타원이 길쭉해 보여 오히려 실격되는 구조였다.
- **불확실성 영역의 중심이 측정에서 나온다.** 이전에는 실제 표적 좌표에 잡음을
  얹어 만들었으나, 이제 방위선 위 추정 거리에 놓는다. 실제 표적을 품는 일은
  포괄성 보장이 맡는다.
- 거리 정밀도에 18% 바닥을 뒀다. 표적도 기동하므로 해가 완전히 수렴하지 않는다는
  근거이며, 이 값이 ESM을 정밀 사격해로 쓰지 못하게 막는다.
- 신호 소실 시 영역 성장이 단일 반경이 아니라 두 반축 모두에 적용된다.

### 수정

- **원이 거리를 암묵적으로 한정하던 문제.** 관측자 (0,0), 실제 거리 9.3km인데
  중심 10.5km·반지름 6.9km라 거리가 3.6~17.4km로 좁혀졌다. 바로 옆 패널은
  "거리 미상"이라 표시해 서로 어긋났다. 수신기 하나짜리 수동 방위 탐지는
  원리적으로 거리를 만들 수 없다.
- **현실적인 좌우 기선이 거리를 전혀 못 만들던 문제.** 함폭 20m 기준 최대
  ~18m인데 명목 12km에서 타원이 되려면 ~19m(q=1) 이상이 필요해, 전후 쌍을 깔지
  않으면 거리가 영원히 나오지 않았다. 판정 기준을 상대 거리오차로 바꿔 16m 쌍이
  약 ±30%로 정상 동작한다.
- **TMA 앵커 시각이 0일 때 관측 경과가 항상 0이 되던 버그.**
  `Number(x) || nowS`가 앵커 시각 0을 falsy로 흘려보냈다. 전투 시작 직후가 정확히
  이 경우라, 방위 시간평균이 사실상 항상 죽어 있었다.

### 내부 구조

- 신규 함수: `esmBaselineForBearing`, `esmSignalRangeEstimate`,
  `esmMotionBaseline`, `esmRangeSolution`, `esmUncertaintyAxes`,
  `esmAreaRangeLabel`, `esmRangeSourceLabel`, `esmBearingPrecisionLabel`.
- 트랙 필드 교체: `esmAreaRadiusM`(참조 18곳) →
  `esmAreaMajorM` / `esmAreaMinorM` / `esmAreaAngleRad`, 그리고 측거 상태
  (`esmBaselineM`, `esmMotionBaselineM`, `esmRangeRelSigma`,
  `esmRangeEstimateM`, `esmRangeSigmaM`, `esmAreaRanged`).
- `esmAreaContainsPoint`가 원 판정에서 회전 타원 판정(국소 좌표 변환)으로 바뀌었다.
- `drawTacticalEsmArea`가 `ctx.arc`에서 `ctx.ellipse`로 바뀌고 쐐기 분기가 생겼다.
  쐐기는 함선에서 뻗어야 하므로 관측자를 인자로 받는다.
- **사격 계약은 건드리지 않았다.** 타원이 성립해도 `rangeKnown`은 false로 남고
  자동사격 하드 게이트도 그대로다. 거리 추정은 표시·판단용으로만 실린다.

### 검증

- 자체 테스트 하네스 **644 단언 통과 / 0 실패 / pageerror 0** (기준선 606).
- 신규 하네스 `__SPACEFRAME_ESM_BASELINE_TEST__` 36 단언. 기선 기하, 세 측거
  경로, 기동 효과, 포함성 스윕(7방위 × 5거리 × 3함형 = 105회, 미포함 0건),
  타원 판정의 판별력, 사격 차단 유지를 덮는다.
- 12km 표적 기준 실측 수렴: 1기 정지 ±42% → 3km 횡기동 ±25% → 좌우 쌍(16m)
  추가 ±20% → 전후 쌍(48m) 추가 ±18%. 미분류 방출원은 거리 미공표.
- 성능: `esmRangeSolution` 2기 0.17µs, 8기(28쌍) 0.61µs.

### 남은 작업

- 기본 설계(`defaultDesign`)에 ESM이 한 기도 없다. 플레이어가 직접 배치해야 ESM
  접촉이 생긴다. 기본 탑재 여부는 미정.
- 육안 확인은 전술맵 렌더 캡처로 대신했다. 실제 출격 플레이 확인은 하지 않았다.
